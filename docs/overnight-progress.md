# 2026-09-08 夜间推进与验收

## 执行约定

长期约定见根目录 `AGENTS.md`。用户已授权自主决定具体方案；阶段完成后必须测试，永久保留研究入口。开始时 Git 工作区干净。

## 阶段方案与任务

1. 基线与静音：补 `--mute`，验证菜单兼容、声音资源检查及现有武器／Arena 回归。
2. 盔甲：核对 ARMOR 模板、装配和属性原版实现，补装备接入与永久研究入口；全目录验证。
3. 真实关卡战斗：核对关卡脚本与刷怪器，打通地图、玩家、敌人、波次和死亡重开；以独立可玩入口验收。
4. 成长与保存：依据原版数据接入必要奖励／装备／进度；原始存档保持只读，明确重建版保存边界。
5. 全面回归与交接：构建、自动检查、典型地图与装备截图、操作说明、待核对内容与命名对照。

阶段 2 之后的细分任务以调研事实更新，不虚构原版机制。

## 已确认的基线

- 已有 16 个菜单入口，含武器查看器和独立 Arena。
- 已有 `CArmor` 解析文件，不代表盔甲装配和属性生效已完成。
- 现有 Arena 文档明确未接真实关卡导航／波次、Boss 特殊机制、击杀奖励；这些仍需调研和实现。
- 实际资源位于当前项目根目录；iOS 程序、旧 PC 版和存档均存在。

## 阶段结果

### 阶段 1：通过

- Release 构建通过。沙箱内 MSBuild FileTracker 被系统拒绝访问；通过已批准的正常构建命令完成，无需修改工程跟踪配置。
- `--mute --weapon-check`：退出 0，76 模板通过，3 项仅展示。
- `--mute --arena-check`：退出 0，78 条目、76 脚本、未知调用 0、失败 0。
- `--mute --arena 0 --fire --advance 600 --screenshot out/overnight-mute-arena.png`：退出 0，截图已目视核对。
- 输入 `14` 到仅带 `--mute` 的 EXE：菜单正常，资源自检 MATCH，退出 0。
- 静音在统一 `CAudioPlayer::Play` 层阻止播放流，保留 WAV 解码和键验证。

### 阶段 2：调研与细分

- `CPlayerConfiguration::SetArmor`（171658）证明模板首字节是 0–3 装备槽，非不明 flag。
- `CArmor::Bind`（176612）证明另两字节为挂点；class 13 的五个 int16 变量在 Equip export 0 中赋值。
- `CBrother::Draw` 证明两个模型并不总是两位兄弟版本：胸甲可同时绘制两个挂件；模型缺失时可使用身体贴图替换。
- 先完成全目录解析／脚本属性调查并保留验收入口，再接身体装配与战斗属性；修正旧推断时保留原注释历史并追加更正。

### 阶段 2：核心通过

- 完整说明见 `docs/armor.md`，菜单新增 17／18／19，原 16 项全部保留。
- `--mute --armor-check`：退出 0，233 件、160 模型引用、失败 0。
- `--mute --armor-render-check`：退出 0，699 组合、失败 0。
- `--mute --arena-check`：退出 0，原回归通过；新增三件套减伤 8.6、攻击 10.5、移动 20.46 均与独立预期一致。
- `--mute --weapon-check`：退出 0，76 模板通过。
- 三张代表截图已目视核对，Release 和 Debug 构建通过。
- 检查自身曾误把脚本控制流返回值当作成功标志；依据解释器实现已纠正，重新检查通过。

### 阶段 3：正在调研

- 用户补充：正式版每个星球只有一张生存地图，四面刷怪，50 波 × 10 轮；战役在 1.0.0 前废弃，恢复属于可选研究。主线据此调整为星球生存模式。
- `flow scripts/` 版本未知，仅辅助解释符号。任何与 iOS/BIG 不一致的行为以当前版本为准。

- `CLevel` 目前只执行地图层／相机初始化；原版 `CEnemySpawner` 另有规则总数、速率、上限、开关和路径调度，需要还原。
- 现有 `CombatScene` 场地边界和位移是 Arena 固定值；接入真地图之前必须变为地图提供的几何／碰撞约束，避免敌人沿空场地规则穿墙。
- `M3Map.cpp` 已含完整地图绘制、玩家移动和摄像机，优先复用，保留 Preview 和 GameView 原研究入口。
- 已新增 `CEnemySpawner` 十规则调度和 `CLevel` 计时／击杀事件，菜单 20／`--level-flow-check` 为不含 AI 的关卡流程测试；正在构建和验证，不能提前标为通过。

### 阶段 3A：基础调度通过，完整 500 波仍待验证

- 修复确定性复现：缺少 `CInputPad::InterstitialSequenceCallback` 发出的 level event 2，导致开场脚本永久等待。测试宿主现显式发送开场／换波过渡完成回调，游戏宿主将从 HUD 完成时发送。
- 补齐 `CGame::VariableResolver`：当前 iOS 的变量 4/5 为联机模式标志、6 为教程阶段；普通菜单在 94113 设为 -1。未知版本 flow 的 SaveData 命名不可采用。
- `--mute --level-flow-check` 退出 0：19 份脚本，四份正式 50/500 脚本均刷怪并推进。300 秒模拟内 pack2/7/12 完成 12 波，pack9 完成 6 波后进入 Boss 专用事件等待。
- 该检查以一秒后发送击杀回调的测试世界验证调度，不代表实际 AI、Boss、奖励或 500 波全部通过。报告列出未实现 native，不能用退出 0 隐去范围限制。
- 变更全局脚本变量后，Arena 回归退出 0（78 条目、未知调用 0、失败 0），武器回归退出 0。

### 阶段 3B：真地图战斗接入方案

- 先保留原 `CLayerPathLink` 节点、连接与锁定状态，核对真实刷怪坐标；其 int16 坐标直接是世界像素。
- 将 CombatScene 的边界、模型缩放和碰撞改为可由地图配置，Arena 默认保持原场地；以真实地图碰撞约束移动和弹道。
- 在现有地图渲染之上接生存宿主、HUD 和关卡刷怪，不另造地图；先验证一图，再扩展星球选择、死亡重开与存档。

### 阶段 3B：首图战斗通过，继续扩大验证

- 菜单 21／`--play` 打开正式 pack2 MAP7 生存游戏；`--map pack7 6`、`--map pack9 0`、`--map pack12 0` 指定其他正式地图。`--survival-check` 使用实际玩家脚本、子弹、敌人 AI 和死亡脚本验证至少两波；测试驾驶员设为无敌，正常游戏默认没有无敌。
- `--mute --survival-check`：退出 0，完成两波，35 次击杀，35 次生成，无无效生成。`pack7 MAP6` 同样通过两波／35 击杀。
- `out/survival-first.png` 已目视核对，真实模型、地图和 HUD 可见。
- 修复测试发现的悬空引用：旧地图换枪函数替换整个 PlayerModel；现原位更换武器，保留 CombatScene 引用、生命和盔甲。
- 原始 `CLayerPathLink` 与 `CLayerPathMesh` 现保留数据，共用 `ILayerPath` 查询；读取四边形、相邻节点，支持包含多边形／共享边通道。当前路线选择为宿主 Dijkstra，尚非原版距离图与 flock 的逐指令移植。
- 重要纠正：不能把玩家墙体推开逻辑用于敌人。`CEnemy::UpdatePathFinder` 在 70142 直接沿导航点更新位置；`TestCollisions`（73079）检查子弹和玩家。额外套玩家碰撞会阻塞原数据的可用网格通道，已移除；玩家和子弹仍受原地图碰撞约束。
- 仍待验证：死亡／重开、跨波 Boss 专门事件、星球选择、存档成长、完整 500 波、兄弟 AI、拾取奖励和可破坏场景联动。

### 阶段 3B：四图与生命周期通过

- 四图 `--mute --survival-check --map ...` 均退出 0，实际击杀 35，完成两波；pack12 还保留一个脚本生成的零半径场景控制对象，不能误删为敌人废案。
- 菜单 22 永久保留真实地图集成测试。增加原位换枪、生命指针、盔甲属性保持、实际致命伤、R 重开清理和重新推进两波的检查。
- `--mute --survival-check --armor 11`：退出 0，装备／死亡／重开失败 0，两波战斗失败 0。
- 生命周期检查曾发现生存模式继承 `PlayerVitals` 的 Arena 无敌默认值，现入口明确关闭，仅自动驾驶阶段打开无敌。原存档未修改。
- 当前生存入口仍是早期可玩闭环。完整 500 波与 Boss、成长奖励、商店／装备选择、兄弟 AI 和正式菜单仍需后续阶段。

### 阶段 4：Boss 与完整波次闭环方案

- 接上敌人原版 native 56 的 level event 转发，补充 Boss 演出和关卡全局上下文；不凭波次数字直接删除 Boss。
- 将测试扩展到跨 Boss 和一整轮，再核对十轮属性倍率和最终结束。
- 完成前不得将“前两波通过”标为完整生存模式验收通过。

### 阶段 4：Boss 缺口和倍率已局部通过，整轮仍在验证

- pack9 第六波 Boss 死后状态 115 等待剩余敌人恰为 2。原地图对象 120/121 是两座常驻炮塔，漏生成时剩余为 0，不能篡改脚本条件绕过。
- 保留地图对象层全局索引与 Enemy extra 的路径／朝向；接 CLevel 的显式 tag 生成和单对象生成、按原始对象 ID 发送消息。普通刷怪 ID 保持 -1，不再误用战斗 UID。地图自动可见区生成及非敌人动态对象仍待接入。
- `CEnemy::HandleMessage`（68024）把消息 0..5 对应到事件 4/9/10/11/12/13。level event 由场景更新完成后转发，避免遍历敌人时重入生成。
- `--mute --survival-check --map pack9 0 --check-waves 12 --weapon 47` 已退出 0，281 击杀，两座炮塔保留，装备／死亡／重开检查通过。该结果早于倍率接入，之后继续回归。
- 首图整轮测试曾卡在隔墙对射。独立 Arena 同武器可以击杀对应敌人，不能记成敌人废案。新增测试专用 `SurvivalPilot` 按实际碰撞绕行和撤退引敌，仍只输入移动／射击，不改敌人血量、位置或脚本。
- 倍率按原版接入：native 50 设血时乘生命倍率（72551）；native 51 读回未缩放生命且至少 1（68315）；Update 以速度倍率缩放完整时步（67783）；子弹、接触及范围伤害使用独立伤害倍率。宿主为已移除发射者保留发射时倍率，避免悬空指针。
- CLevel 三个模板字段已依据读者命名为 `wavesPerRevolution`、`waveLimit`、`perfectWaveRewardPercent`，旧注释保留并追加更正。
- `--mute --arena-check` 退出 0：78 项，未知 native 0、失败 0；新增独立数值契约生命 15／脚本生命 5／移动 20／伤害倍率 3 通过。`--mute --level-flow-check` 19 脚本回归退出 0。
- 当前仍不能宣称完整 500 波通过；整轮、后续倍率、生存结束和成长界面仍在推进。

### 阶段 4：首图一整轮、跨轮及终局局部通过

- `--mute --survival-check --check-waves 50 --weapon 47` 退出 0：50 波、2930 击杀、2941 生成、无效生成 0、失败 0。生成与击杀差额包含脚本主动移除对象，不强行补杀。
- 测试驾驶员的碰撞绕行、部件瞄准、撤退和稳定目标选择已通过上述实际战斗；属于测试输入工具，不能当作原版兄弟 AI。
- `CEnemy` 变量 20 原版调用 `GetRealWave`（114232），即轮内零基波号；累计波号单独保留。`CLevel` 变量 4 是偏移 311032，非 311028 的终局上限，已纠正初始化。
- 菜单 23 永久保留波次研究；`--start-wave 1..500` 在 OnLevelStart 前设置进度，依据原版 `CGame` 调用 `CLevel::SetWave`（76837），继续运行同一份关卡脚本。
- `--mute --survival-check --start-wave 50 --check-waves 3 --weapon 65` 退出 0：累计到 52 波，444 击杀。原先每波两分钟的总测试限时不足，现放宽后期波次模拟时限，并以两分钟没有实际伤害进展判停。
- `--mute --survival-check --start-wave 498 --check-waves 3 --weapon 65` 退出 0：累计到 500 波，720 击杀，结束状态通过。`out/overnight-survival-complete.png` 已目视核对；这不是连续跑完全部 500 波的证明。
- 正式桌面生存按用户确认的 50×10 在清除 500 波后结束，停止调度并显示完成，不显示第 11 轮。原二进制的 `waveLimit-1` 与结算比较有潜在边界差异，保留待核对说明。

### 阶段 5：成长、装备选择和保存调研计划

- 读取 `CPlayerProgress` 的原版经验／生命曲线，以及 `CStoreItem` 的价格、名称和引用；先做资源完整性检查，再接界面和购买／装备行为。
- 追踪敌人击杀、Xplodium 拾取、波次奖励的归属与单位，避免把所有奖励简化成凭空增加金币。
- 重建版进度写入独立目录，原始 `saves/` 不修改；研究入口可自由选物品，正式成长与研究选择明确区分。

### 阶段 4 补充：四图整轮通过

- pack7 MAP6：50 波、2986 击杀／生成、无效生成和失败均 0；pack9 MAP0：50 波、3078 击杀、3123 生成，保留两座炮塔；pack12 MAP0：50 波、2868 击杀、2875 生成，保留场景控制对象。各自日志见 `out/overnight-survival-pack*-50.log`。
- 第二星球第 41 波复现阻塞后，发现 M3 查看器合并所有对象层的行为不适用于正式游戏。现 `LoadProps` 根据原 `CLevel` 选中的对象层加载；pack7 道具从 106 减为正确活动层的 98 个。定点第 41 波和随后完整 50 波均通过。
- 其他地图整轮结果早于此对象层过滤修正，继续做新版回归。首图连续 500 波正在独立旧构建中运行，不能提前记录为通过。

### 阶段 5A：成长表与商店资源通过

- 新增原名 `CPlayerProgress`、`CStoreItem`，完整读取模板尾部和商品全部引用／属性组；未知字段保留原偏移名。
- 菜单 24／`--mute --progress-check` 永久保留。Stage5 构建退出 0：200 级经验／生命边界、339 商品、396 引用，失败 0。逐级报告与商品名称／价格见 `out/progress-check.txt`、`out/store-check.txt`。
- 玩家第 1 级生命 6，升第 2 级需要 15 经验、生命 9。按实际 `CPlayer::AddExperience` 的 `>=` 边界升级；`CPlayerProgress` 存盘辅助代码存在 `>` 差异，后续保持注明。
- `CLevel::OnEnemyKilled`（119609）证明敌人模板 112 为经验、114 为 Xplodium；已修正 `EnemyModel` 与旧调查器字段名称。关卡倍率顺序 0 伤害、1 生命、2 Xplodium、3 经验、4 速度。
- 实战成长正在接入。Xplodium 必须经原炼化系统成为金币，不能直接把击杀奖励伪称金币；原存档仍未修改。

### 阶段 4 最终补充：首图连续 500 波通过

- 原命令 `--mute --survival-check --check-waves 500 --weapon 65` 退出 0：28948 击杀、28949 生成、存活 0、无效生成 0、失败 0，实际连续推进十轮并进入完成状态。日志 `out/overnight-survival-pack2-500.log`；截图副本 `out/overnight-survival-500-continuous.png`。
- 此构建包含活动对象层修正与关卡倍率，但早于本阶段成长接入；自动驾驶无敌，未篡改武器或敌人数据。正常游戏不无敌。

### 阶段 5B：成长、炼化、购买与保存通过

- 实战两波检查与盔甲 11 回归均退出 0：35 击杀、35 XP／Xplodium，等级 2、生命 9。升级保持原生命比例的数值检查通过。
- `CRefinementManager` 完整读取 12 档原数据；免费即时 100%，5 分钟 120%、15 分钟 150% 等。核对时间、解锁价格、前置档位、收取一次及序列化后继续炼化。
- 新增 `CPlayerConfiguration` 原双枪／四甲槽默认值；`CProfileManager` 使用独立 `GUNBROS_RE_PROFILE 1` 格式。购买按等级、金币优先否则 Warbucks、授予资源；重复拥有不扣款、不交付不支持的消耗品。写入完成后原子替换，坏文件不会部分覆盖内存进度。
- 菜单 24 现在同时验证购买、存档重载、损坏输入和炼化；全部通过。菜单 25／`--mute --profile-play-check` 永久保留真实存档流程：玩两波→保存→重载→续玩至四波，99 XP、82 Xplodium、失败 0。测试仅写 `out/`。
- 正式进度上下文已连接战斗，限制为已装备双枪／盔甲，在换波、死亡、重开前和退出保存；研究入口继续自由选枪，不写正式进度。正式图形菜单尚未完成。

### 阶段 6：正式图形入口方案

- 主界面提供星球、装备、商店和炼化；星球大图、名称及商品图标取自原版资源。保存到 `userdata/profile.dat`，与 `saves/` 原件完全分开。
- 视觉以原星球图为中心，保留 4:3 布局；钢蓝背景、灰蓝面板、浅灰正文、琥珀操作提示。像素标题和紧凑正文用于当前宿主；并非宣称原菜单电影／字体系统已经移植。
- 先验证菜单截图与交互，再验证正式入口到战斗和返回；之后继续兄弟 AI、道具联动和特殊关卡。已有 25 个菜单入口永久保留。

### 阶段 6 结果与阶段 7 计划

- 菜单 26／`--game` 已实现正式图形入口，默认回车进入；原有 1..25 全部保留。四页截图 `out/game-menu-0..3.png` 已目视核对，修正 `%` 字形与炼化前置条件提示。
- 菜单 27／`--mute --game-menu-check` 持久保留。通过按钮命中测试完成：250 Xplodium 即时炼化→225 金币买 Mad Dogs→免费 ER97E Elite→双枪装备→选择 Haven→实战两波→重载，金币 25、库存 6 项、等级 2、失败 0。该输入驱动经过真实按钮处理，但不是外部操作系统鼠标自动化；账户只在 `out/`。
- Release 与 Debug 构建通过；Release 回归武器 76、Arena 78／未知 native 0、盔甲 699 组合、关卡脚本 19，全部失败 0。尚未验证联机、在线服务或原版菜单电影和字体系统。
- 下一步先补原版 `OnWaveCleared` 玩家回调及无伤波次奖励，再接 `CBrotherAI` 的跟随／瞄准／受伤生命周期，最后补道具与拾取联动。每项独立验证后再整合，不重写已通过的武器和关卡脚本。

### 阶段 7A：波末奖励与角色回调通过

- CLevel native 47 在加波号前调用 CBrother export 6；按 `OnWaveCleared`（116980）把本波新增 Xplodium × 模板百分比向下取整，最少 1。受伤取消本波奖励，奖励自身不进入下波基数。
- 独立临时场景验证空波最低值、前波奖励排除、受伤取消和下波重置；实战三波奖励 1/3/1、42 击杀、49 XP、47 Xplodium，失败 0。菜单购买／炼化／双枪／Haven 两波存档回归通过。

### 阶段 7B：AI 兄弟独立战斗与复活通过

- 原名 `CBrotherAI`：跟随玩家 130 起步／100 停止，100 世界单位/秒再乘盔甲速度；独立瞄准 200 获取／220 保持、0..500ms 反应、初始 ±25° 误差向 ±5° 收敛。导航接现有原网格；随机数使用宿主固定种子，原版共享 RNG 序列未逐指令移植。
- 兄弟拥有独立生命、角色／武器脚本、子弹归属、受击和击杀；公用弹体每 tick 只更新一次。死亡运行到 state 8，export 6 进入原复活脚本，恢复生命／移动／射击，不能直接重置当作复活。
- 原虚表核验：armv7 slice 0x78F000，CBrother 表 0x403EF8、AI 表 0x4040F8，+220 是 `GetBrotherType`（AI 返回 1），不是 IsPlayer。`OnEnemyKilled` 的 AI 击杀共享 XP，但不产生人类玩家的 Xplodium 连杀奖励；首轮 AI-only 两波 35 XP／0 Xplodium 是原分支结果。
- 纠正玩家脚本变量 2：人类为 1，AI 为 0；过去宿主默认 0。setHealthPercent 必须允许复活死者；控制开关和脚本有限无敌时间也接入。
- 菜单 28／`--mute --brother-check --weapon 65` 永久保留。四主图各两波仅 AI 射击、原死亡／复活均通过；武器 47 光束加盔甲 11 同样通过。日志 `out/stage7-brother-0..5.log`；独立单人回归通过。
- 正式离线入口使用本地兄弟，暂沿用玩家进入关卡时的主枪、盔甲和等级；不是原在线好友装备同步。AI 随机换枪、精确原距离图、完整复活范围冲击尚待补齐，不宣称 AI 全部细节完成。正式入口与 HUD 整合后继续回归。

### 阶段 8：拾取物与场景联动方案

- 先完整读取 CPickup 模板、原收取脚本及图像／效果／商品引用，验证只能收取一次，再接实际碰撞、绘制和刷出位置。
- CEnemy native 62 实为 SpawnItem→CEnemySpawner::SpawnPickup（68644），需要生成原拾取物，不能重复当成击杀 XP。Spawner 18..21 的位置参数按原函数逐项实现。
- CPickup native 0/1/2 为 Xplodium／经验／生命点数，3 播音效，4 授予列表中的商店物品。回血为点数相加并封顶，不是百分比。任一兄弟收取时奖励仍指向人类玩家（99742）。
- 拾取阶段通过后，再追踪 CProp 消息和活动对象层的动态碰撞变化；每项继续保留独立 EXE 研究入口。

### 阶段 8 结果：拾取物数据、画面与实战通过

- 原名 `CPickup` 完整读取 9 模板，执行 13 条收取动作，资源引用和重复收取检查失败 0；菜单 29／`--pickup-check` 永久保留。
- 菜单 30／`--pickup-render-check` 显示全部九种原图并检查全部动画帧；`out/pickup-render-check.png` 已目视核对。四张钥匙卡与武器箱目前只有原模板收取音效，不能编造额外奖励。
- 独立实战场景验证 +20 生命封顶、+150 Xplodium、+500 XP、手雷商品免费授予一次；手雷存入原 powerup 13 引用。重建存档格式升级为 2，仍兼容格式 1；原 `saves/` 未修改。
- 首图 12 波真实调度：native 18 在第 4 波起调用，参数 `16,8,-1,500`；刷出 3 项、收取 1 项、剩余 2 项、失败 0。此前三波没有生成是原调度时机，不能据此判断刷出失败。`CEnemy` native 62 的手雷掉落同样已观察到。
- 新版 pack9 与 pack12 各完整 50 波均退出 0，已包含成长、拾取、活动对象层；分别 3073／2876 击杀。日志 `out/stage8-pack9-50.log`、`out/stage8-pack12-50.log`。双人正式菜单回归、光束兄弟回归、成长和 19 关卡脚本均通过。
- 仍待补：收取粒子特效、原最近十项拾取记录和消耗道具使用；拾取位置使用原节点，未逐指令移植原空闲位置评分。

### 阶段 9：机关脚本与动态碰撞

- 原 `CProp` native 5/6 按边组关闭／打开碰撞，消息 0/1/2 对应事件 2/3/6；计时器、受击、进入／离开区域也由脚本驱动。正式场景改为每实例脚本；查看器原展示操作继续保留。
- 新增原名 `CMoveSet` 读取完整尾部。零动作表在 BIG 中保留一个零分配字，iOS 读者提前返回；宿主读掉这个可核验的填充，不把它误报为解析剩余。
- 菜单 31／`--prop-check` 已加入。初次完整检查为 264 成功、1 失败；失败项 `pack9 PROP 43` 尾部 `89 75 26 00 02 00 04 00 00 ff ff ff ff`，声明四项但不足以按 iOS 读者读完。保留失败及原字节，正在核对地图引用，不把缺数据强补成成功。
- 原 `CProp::IsDone` 依据模板移除标记与生命值；脚本销毁事件并不总是移除场景残骸。保留历史字段注释，并追加原读者依据。
- 机关脚本已接原角色碰撞／子弹碰撞边组、消息、受击、计时、区域进入及关卡 export 3/8。静态展示入口仍保留原手动切换；正式入口使用实例脚本。原动作帧读取音效与播放时钟共用 `CSpritePlayer`。
- 三张图 12 波通过：首图实际机关受弹 14 次，Yeroc Sina 9 次；Ceres 2 两个机关收到原关卡消息。独立受伤／计时／动画分别验证 14 与 9 个场景实例。原异常 `pack9 PROP 43` 在全部地图对象层中未找到引用；完整资源检查仍显式退出 1，不伪装全目录通过。
- Haven 初测第 8 波的失败属于测试驾驶员选中两个节点的小片段。记录可达节点数后改为以玩家可直达的所有邻近节点作为规划起点，仍经真实碰撞移动；原 12 波重跑通过，283 击杀。日志 `out/stage9-haven-route-fixed.log`。
- 原子弹到达障碍后过早执行墙体销毁回调，导致机关无法收到受击。现在先在已被墙截断的线段内处理真实目标，再执行剩余墙体回调；76 武器及 78 敌人 Arena 回归均通过。正式购买／炼化／双枪／双人战斗菜单检查通过。
- Haven 后续整轮再次失败，继续诊断发现实际爆炸击退错误：宿主把大地图坐标裁到了 Arena 的 1200×900 边界，可能挤入障碍。原 `CBrother::OnSplashDamage`（135359）使用持续 `SetForce`；`CEnemy::OnSplashDamage`（67945）只交脚本处理，不直接移动坐标。已按这两个分支修正，并增加大地图逐帧位移契约（立即不位移，16ms 位移 1.6）。先前驾驶员修正仅解决路径采样问题，不能代替这个游戏层修复。
- `out/stage9-haven-force-fixed.log`：新版连续 50 波，2927 生成／击杀，无效生成 0、失败 0；首图 `out/stage9-prime-50.log` 同为 50 波通过（2863 击杀），后者早于持续击退修正。各阶段的失败日志保留。
- `CGameSpriteGluRef` 声明和实现从 `CProp` 移回 `CGameAssetRef`，对应原 `src/gunbros/gameAssetRef.cpp`；原文档注释随类移动，历史 include 注释保留并追加更正。

### 阶段 10 方案：消耗道具与手雷

- 先完整读取原 `CPowerup::Template` 和实际脚本，核对使用／装备导出、计时、BUFF 以及手雷资源引用；新增永久检查入口。
- 优先完成已能拾取并保存的 F.R.A.G. Grenade：装备、使用动画、抛出和真实范围伤害，验证仅消耗一次及重载数量。再按资源证据接生命／护盾／狂暴等道具。
- 菜单电影与在线功能单独标明宿主替代或未实现；不以虚假的充值／奖励流程替代原数据。原存档继续只读。

## 废案与待核对

### 阶段 10A 结果：原手雷动画与扣量通过

- 原 `CPowerup` 全部 20 模板已读完；新菜单 32／`--powerup-check` 永久保留。实际脚本确认 native 24 设置弹体、25 请求抛出，装备导出 5、使用导出 6/7。
- `CBrother::Update` 原事件 10/11 驱动投掷动画，native 14 真正生成弹体后才扣库存。抛射速度按常量 1138163712 核实为 430，起点取身体节点 2，未从枪口偏移猜测。
- `out/stage10-grenade-play.log`：普通／电击／冰冻三种手雷各 1 个真实弹体、库存 2→1；重复请求拒绝、死亡前未抛出不扣、满血不消耗回血包、实际回血封顶，全部失败 0。首图两波回归同为失败 0。
- 实战新增 G 使用、F 切换已持有道具；已支持三种手雷和三档回血。原片段中 `CBrother` native 13 复活范围冲击也接真实范围伤害；尚需下一轮双人回归。
- 原电影引用类型 253 不能套对象 section+1；已追到 `CMovie::Init`（109521）的 `GLU_MOVIE_MOVIE` 命名基址，初次 7 个失败正在按正确路由复核。原菜单电影播放和镜头震动尚未移植。

### 阶段 10B：属性道具、商店与研究入口

- 电影按原基址路由后 20 模板／35 引用／失败 0。`--powerup-check` 的菜单动画完成事件为明确的研究回调，不能视为已移植原菜单电影。
- 护盾 10 秒，三种独立强化 15 秒；攻击／防御倍率实际为 332/256，速度为 384/256。原 `HandleDamage` 先除防御强化倍率，`OnMove` 乘速度倍率；换枪保留角色道具计时。库存请求、实际抛出、存档分别验证。
- `CBrother::FireBullet`（136422）明确依次读取三种强化，后类型基于原基础伤害替换前类型倍率，防御和速度也影响发射伤害。保留此原行为及研究证据；旧 `StartFrenzy` 仅见写入独立三参数，未在当前伤害／移动读者中找到作用，Tantrum 暂不放入可购买范围。
- `out/stage10-buffs-play.log`：护盾拒伤／重复使用拒绝／换枪计时保持／到期恢复，强化读取／优先级／到期，真实库存保存重载均失败 0。`out/stage10-store-check.log` 验证 10 枚手雷整包重复购买、余额不足、超量消耗拒绝与格式 1→2 兼容，200 级／339 商品／396 引用全部失败 0。
- 新菜单 33 为永久道具实战研究，临时库存每种已支持道具 10 项；不写正式账户。`--powerup-play-check` 同时运行消耗／角色／实战回归并生成道具效果画面。
- 图形菜单新增 ITEMS 分类，整包数量和已持有数量明确显示。G 使用、F 切换；正式道具价格全部取原 STORE。原素材／用户存档均未修改。
- 道具原粒子效果截图 `out/powerup-play-check.png` 已核对；菜单截图发现原 Bro-op grenade prize 奖励记录不能当成无限免费商品，已从购买范围排除，仍允许原拾取／奖励授予。价格为零的消耗品仅保留研究与奖励路径。
- 普通弹体接触伤害保存发射时强化倍率；原范围伤害 native 重新读取施放者的盔甲／关卡倍率，不乘弹体接触强化。两条路径分别处理。
- Release／Debug 最终构建通过；道具商店通过真实按钮处理连续购买两次并重载，库存 2、金币 0。验收截图追加空输入帧，确保画面显示本次扣款后的余额；不会把绘制前旧余额当作最终截图。

### 阶段 11 方案：弹道落地与拾取常驻效果

- 核对原 `CBullet` 模板轨迹强度／时长／类型、首段结束事件 class 8 event 3，区分投掷请求成功和真实爆炸／控制效果。补齐后用原资源检查，不只数生成的弹体。
- 原 `CPickup::Spawn`（99877）生成常驻粒子，在收取时 `StopSpawning`，并非收取后才播放一次特效。按这个生命周期接入，保留已经发出的粒子自然结束。
- 继续验证已完成的地图、道具和菜单；既有研究入口全部保留。

### 阶段 11 结果：弹道、爆炸与拾取效果

- 原轨迹三类阶段表和 class 8 event 3 接入实际更新；电击手雷 320ms 首次落地，随后 1312/2320ms 脉冲。普通／冰冻在 608ms 各一次爆炸（16ms 步进）。轨迹高度控制碰撞开关；type 2 后段减速度原常量解码为 -150。
- 原 Draw 中影响移动的后段减速移至模拟更新，确保截图与无绘制检查行为一致。视觉已接伪高度放大／透明度；原随机旋转轴、完整位置偏移尚未逐项移植。
- 墙体反射按 flag 0x800 保留弹体并反射方向，再执行原事件 2。`--powerup-check` 固定检查原三手雷脉冲次数、时机、轨迹峰值、墙体存活；20 模板／35 引用／失败 0。
- 真实 `CBullet → CombatScene → CEnemy` 伤害验收：普通／冰冻各 100，电击三次合计 30，原受击脚本处理，失败 0。目录 0 敌人未显示控制状态，不能把上述伤害通过说成眩晕／冰冻完成；继续核对原控制器与敌人分支。
- 五类拾取效果原发射器均为 start/end=-1；按 CPickup 生命周期启动、收取时停止发射并保留活粒子。菜单 30 的九种物品截图已核对，2 秒保持 5 组发射器，重置后停止发射、已有粒子自然耗尽，失败 0。
- 原 native 9 实际是 ZOrderGroup（字段 +448，GetZOrderGroup :64007），历史 ricochets 命名已更正为 zOrderGroup，保留旧注释并追加证据。精确分组绘制仍待补。
- `out/stage11-weapons.log` 76 武器通过；`out/stage11-arena.log` 78 敌人、76 脚本、未知调用 0、失败 0。`out/stage11-impact-play.log` 两波、道具库存／伤害／到期／死亡回归通过。
- 阶段 10 长跑 `out/stage10-pack9-50.log` 与 `out/stage10-pack12-50.log` 均完成 50 次清波，失败 0；此时关卡脚本可能已经生成下一波，不能将日志的 alive 2/1 描述为清场 0。

### 阶段 12 方案：受击属性与控制器核对

- 对全部敌人实际脚本分别输入普通／电击／冰冻属性，记录状态、伤害和未实现调用，先区分资源版本差异与宿主缺失。
- 核对 CStunController 原计时／结束回调以及 CEnemy native 66/67，仅据实际证据接入，不把名称当作机制。
- 第一张图的阶段 11 完整 500 波测试同时运行，使用独立 EXE 副本；后续行为变化需要另行注明覆盖范围。

### 阶段 12 结果：原敌人控制效果通过

- 成熟敌人的原受击脚本实际触发 native 66：电击 `(192,1)` → 750ms／2px 抖动；冰冻 `(512,0)` → 2000ms／不抖动。出生动画中的普通受击分支不能代表正常行动状态，因此补充了成熟后检查。
- 新增原名 `CStunController`，native 66/67 接原控制计时；`CEnemy::UpdateStun` 保留脚本函数／事件计时，冻结位置、朝向和模型动画。到期恢复，渲染抖动不移动碰撞坐标。
- iOS armv7 虚表 0x402A08/0x402A0C 对应 0x397A0/0x397A8，两个原 CEnemy 回调均为 ARM `bx lr`；不能编造控制结束脚本事件。
- `out/stage12-stun-check.log`：三类真实弹体分别验证 0／750／2000ms，伤害仍为 100／30／100，全部失败 0。
- `out/powerup-enemy-attributes.txt`：76 个有脚本敌人 × 3 属性 = 228 组；44 个模板分别响应两类控制，其他模板保留原分支。位置／模型动画冻结、到期恢复、未知敌人调用 0。无脚本目录 15/62 不纳入这个分母。

### 阶段 13 方案：战役档案与独立研究入口

- 按原 `Mission` / `MissionObjective` 类名读取全部条目，严格验证完整记录、关卡／地图／目标引用，保留独立菜单 34。
- 从当前 BIG 的实际目标和地图脚本确定可恢复任务，若引用缺失则记录为档案问题；未知版本 flow 不补写成事实。
- 有完整脚本的废弃战役独立试玩，与正式四星球生存／账户进度分开。继续复用已验证的角色、武器、敌人和机关，不另造战斗系统。

### 阶段 13 结果：六条战役可独立试玩，完整通关仍待恢复

- `CLevel` 接原 32 组触发开关、暂停计时、export 6；战役玩家移动扫过原触发碰撞边。邻近生成采用当前镜头外扩 200，Manual 标签禁止自动生成，已生成物件不重复。
- 原导航网格区域锁／三点解锁接入，算法从 `CLayerPathMesh::PropogateNodeLock` / `UnlockNodesBetween` 的反编译控制流移植；反编译遗漏的递归实参尚待汇编逐指令复核。不是一般最短路锁定。
- native 40 显示原字符串，第三参数是自动关闭；完成调用 class 4 event 4。文字框、阅读时长是桌面适配，未恢复原电影／肖像／逐字分页。native 22 后继 LEVEL 引用已保留，实际跨地图切换仍未接入。
- 菜单 34 检查 61 任务及六战役地图合同：Manual 开关、远处道具不误生成、重复生成、触发禁用和 1000ms 暂停边界均失败 0。`out/stage13-contract.log` / `out/mission-check.txt`。
- `--campaign-check <pack> <mission>` 使用真实移动／开火、原拾取和触发边，检查人物无敌仅用于自动试跑。六任务均实际生成并击杀敌人：pack2:10=173、11=79、12=1329、13=3、14=9；pack7:0=2。pack2:12 达到 42 波。
- 上述检查只证明战斗与局部流程能运行，**六任务均未验证完整通关**。自动导航未到达的路点、救援标签、电影事件、部分镜头／音效函数、跨 LEVEL 转场继续保留研究；不能把缺实现称为已确认的原版废案。实测图保存在 `out/campaign-check-*.png`。
- 正式 Haven 两波和全部道具属性／库存回归再次通过：`out/stage13-survival-regression.log`、`out/stage13-powerup-regression.log`。后者也覆盖新增 CStunController 单项计时断言。

### 阶段 14 方案：伤害语义与交付回归

- 核对弹体 native 23 的百分比范围伤害，区分玩家／兄弟与敌人接收逻辑；使用不同最大生命值验证，避免把百分数当固定伤害。
- 保留战役档案，回到正式游戏的属性、存档、菜单与输入闭环，继续检查新功能组合。

### 阶段 14A 结果：百分比爆炸伤害

- `CBullet` native 23（原 :61379）使用 authored damage × 施放者关卡倍率，并携带百分比标志；`CBrother::OnSplashDamage`（:135359）再乘受击者最大生命值 × 0.01，随后照常应用盔甲／强化防御。原 `CEnemy` 接收路径不做此百分比转换。
- 属性从 GunCue 经 WeaponEffects、CombatHit 保留到真实范围伤害。最大生命值 20／100／205 分别受到 2／10／20.5 点伤害；玩家和兄弟均通过。`out/stage14-brother-splash.log` 两波、死亡／复活／道具回归失败 0。

### 阶段 15 方案：原存档只读解析

- 原 `CProfileManager::LoadFromDisk` / `SaveToDisk`（202880／203163）证明存档采用版本、填充长度、随机填充、结构体数据、用户号和 CRC；不是加密数据。
- 先建立永久只读检查入口，逐文件验证外层结构、CRC、原数据类型与字段。存档原件保持只读，导出写 `out/`；结构确认后再决定独立导入研究账户。
- 填充长度公式包含 `saveSize >> 1`，仅凭外层只能推断偶数长度下界，精确数据长度必须依赖各类原读者，不能把填充误当字段。

### 阶段 15 结果：原存档解析与独立试玩通过

- 菜单 36／`--original-save-check`：20 文件结构及引用验证通过，原 CRC 全部匹配。原 `CCrc32` 是高位左移、非反射算法，不能使用 zlib CRC32；新 `engine/CCrc32.h` 按 :357828 移植。
- `CPlayerProgress` 的 48 字节记录：200 级、13,811,106 XP、114,514,000 金币、191,975,786 Warbucks、19 Xplodium。与当前等级表计算结果一致。
- `CPlayerConfiguration` 120 字节：主／副枪 pack5:64/38，腿／身／头 pack4:178/177/176。空第四槽保留 core hash＋255，转换时规范化为空引用；不能当成一件未拥有盔甲。
- `CPurchases` 9 条原记录，8 件数量大于零的装备，另有数量 0 的手雷。`CMissionWaveStatus` 五条，四正式星球＋BOKOR各记录500；BOKOR不导入正式四星球进度。
- 菜单 37／`--original-profile` 首次导入到独立 `userdata/original-profile.dat`，之后继续该副本；菜单 26／`--game` 仍使用新手 `userdata/profile.dat`。原炼化队列／社交／成就等尚未转换，报告保留对应原始区块。
- 菜单 38／`--original-profile-check` 在 `out/` 中导入、重载、装配原双枪／盔甲，实际两波35击杀，保存再次重载，失败0。`out/stage15-original-play.log`。真实来袭百分比伤害同时验证了这套60%防御的减伤结果。
- 原文件校验与导出见 `out/original-saves/report.txt`；源 SHA256 清单已写入同目录。正式新手账户尚未被测试创建。
- 高伤害主枪会在敌人进入兄弟200px索敌范围前击杀；因此“AI必须获得目标”的断言只保留在 AI 独立测试，人类同时开火的账户检查不作此错误假设。

### 阶段 16 方案：装备决策信息与验收入口

- 装备／商店页显示选中盔甲的五项原脚本属性，让购买和换装可判断；用原存档组合截图核对。
- 准备可双击的正常／静音／原存档研究启动入口，更新当前功能说明和待研究清单。
- 交付前进行 Release／Debug 及既有研究菜单回归；继续记录每项检查的实际覆盖范围。

### 阶段 16 结果：启动与原存档边界

- 装备属性页截图 `out/original-menu-armor.png` 已核对。四个根目录 CMD 启动器和 `docs/acceptance.md` 已建立，正式静音启动器实测截图 `out/stage16-launcher-muted.png`；未创建正式账户。
- `out/stage16-debug-build.log` Debug 成功。原存档检查器增加五份损坏副本，覆盖短头、截断、非零头字段、异常填充和 CRC 损坏；失败读取不替换已有记录。修正 CRC 错误的退出码和异常长度仍导出的问题。
- `out/stage16-original-boundaries.log`：20 份源记录 CRC 全匹配，五边界检查失败 0。所有损坏副本写 `out/original-saves/`。
- 当前阶段 EXE 独立副本已开始 Haven / Ceres 2 / Yeroc Sina 各 500 波长跑，日志 `out/stage16-pack*-500.log`；结束前不预报通过。

### 阶段 17 方案：原自动瞄准道具

- 已核对 `CPlayer::UpdateShooting`（:101312）、`CBrother::StartAutoFire`（:137216）和 `CTargetingController`（:223185）。玩家必须持续按下射击才搜索目标，native 22 的时长为整数秒，原道具 90 秒。
- 新增原名控制器的 type 2 敌人搜索路径：获取 200／保持 220、反应延迟、误差收敛、丢失目标后的短暂保持射向；不影响已经验收的兄弟 AI 路径。
- 接入实际角色计时、效果、库存和换枪保持；先单项边界检查，再实际场景射击／松开／到期回归，通过后更新可玩道具说明。

### 阶段 17 结果：自动瞄准加入可玩道具

- `out/stage17-targeting-check.log`：20 道具模板／35 引用、获取／保持／释放／移除目标边界失败 0。
- `out/stage17-autoaim-final.log`：闲置 0 发、按住后 5 发／2 命中、松开不再增加、换枪保持、90 秒末尾 1ms 到期边界全部通过。真实场景使用普通步枪与成熟敌人，设定高生命和固定位置作为测量目标。
- 首轮失败来自敌人预热 1000ms 已积累的一发攻击动作，清除预热队列后重新执行；没有改变实测阶段的攻击或断言。完整道具、伤害、控制、死亡／重开和两波回归仍失败 0。
- 使用原 `CTargetingController` 名称与 type 2 行为；随机数是可重复的宿主序列，并非原全局随机序列。兄弟 AI 原有实现未在这阶段重构。

### 阶段 18 方案：受击颜色和混合状态

- 500 波结束截图暴露敌人纯红色和 HUD 背景不清楚。原 `CEnemy::Draw`（:67499）明确将部件受击强度乘 0.5，再用白色 RGB；当前错误复用了枪管过热的红色叠加。
- 粒子 `CQuadBatch::Draw` 最后一个加色分组会遗留混合状态，后续模型／HUD 未重置。修正批次结束恢复普通 alpha，保持粒子自身的三种混合模式。
- 更新受击渲染与结算文字底板，验证加色粒子之后实际 GL 状态、对比截图，再回归地图和菜单。

### 阶段 18 结果：视觉修正与三图长跑完成

- `out/enemy-hit-flash-check.png`：同一原模型左侧正常、右侧 native 29 受击，已目视确认白色半强度叠加，纹理仍可见。枪管过热继续使用红色。
- `out/stage18-yeroc-finale.png`：最后两波到 500，HUD 暗底、结算底板正常。实际加色粒子绘制后读取 GL_BLEND_SRC/DST=0x302/0x303，失败 0。
- `out/stage18-arena-check.log`：78 条／76 脚本、未知调用 0、失败 0；`out/stage18-weapon-check.log` 76 武器通过；`out/stage18-armor-render-check.log` 699 组合通过；`out/stage18-menu.log` 菜单购买／炼化／换装／两波／原存档盔甲截图通过。
- 阶段 16 独立版本三图各完成 500 波：Haven 29,510 生成／29,503 击杀／存活 0；Yeroc Sina 28,767／28,677／0；Ceres 2 31,806／31,160／2。三图无效生成与检查失败均 0。对应日志、截图 `out/stage16-pack{7,12,9}-500.*`。
- Ceres 2 的两条存活记录均为原地图 pack9 enemy 0 炮塔，保留原脚本状态。阶段 18 从第499波开始的 Yeroc Sina 两波检查保留一条 pack12 enemy 2 植物对象。关卡清除与全部 ENEMY 类对象删除不是同一件事；HUD 当前计数仍包含这类地图对象。
- 全程源存档 SHA256 对照清单无变化，正式 `userdata/profile.dat` 和原存档研究副本尚未被检查创建。

### 阶段 19 方案：投放炮塔与弹体生成参数

- 原道具 pack5:19 投掷 bullet 116，后者通过 native 22 生成 ENEMY。当前弹体宿主丢失 native 22 的对象编号／强制池参数，敌人 native 71 的所属玩家炮塔标志也尚未接入。
- 先打印当前 BIG 的真实生成引用、参数与时机，再接原 `CBrother::IsTurretActive/SetTurretIsActive` 和脚本回调。强制池标志仅保留元数据；宿主没有原固定容量对象池，不能假称已恢复原池调度。
- 验证抛出才扣库存、只能一个活动炮塔、原敌人脚本独立索敌射击、结束后重新可用，并保留在道具研究入口。

### 阶段 19 结果：原投放炮塔可玩

- 当前 BIG 的 bullet 116 在 112ms 通过 native 22 生成 pack5 ENEMY 1，对象编号 -1、强制池标志 true。额外参数保留并传至生成场景；原固定容量池尚未复刻。
- `CBrother::UsePowerup`（:138000）对 pack5:19 直接检查并提前设置炮塔活动标志；当前脚本 CanUse 始终 true，不能只查询脚本。原 `IsTurretActive/SetTurretIsActive` 与敌人 native 71 已接入。
- 首轮抓到重复投放两座，核对上述 CBrother 外层保护后修正。取消未完成投掷时释放预约，库存继续保留；实际抛出后由炮塔原脚本控制结束。
- `out/stage19-turret-final.log`：112ms 首座、16,768ms 活动标志关闭、峰值 1 座；97 个弹体包括 1 个投放弹＋96 个射击弹，96 次命中。剩余库存 1、重复使用／换枪取消／死亡取消／到期重用均失败 0。这里是固定目标测量结果，不把 16,768ms 当作所有场景的固定寿命。
- 自动瞄准、回血、强化、三手雷、敌人属性、死亡／重开、两波和实际 GL 混合状态继续通过。道具研究截图新增活动炮塔，HUD 显示强化剩余时间与炮塔活动状态。
- `out/stage19-debug-build.log` Debug 通过；此后增加的取消测试／HUD 需要最终 Debug 增量构建。当前阶段独立 EXE 已开始 Prime、Ceres 2 两图完整 500 波回归，退出码将单独写入 `.exit`。

### 阶段 20 方案：菜单内查看已装备人物

- 装备页目前只有商品图标和属性，查看三件盔甲组合仍要入关。复用已经验证的 PlayerModel，在左侧空白区域显示当前装备人物，购买／换装后即时更新。
- 预览只反映已装备配置，保留商品选择、价格与属性的含义；进入装备页时优先选中当前穿戴条目。
- 用新手账户和原完美存档截图核对布局，菜单按钮流程、存档和完整战斗继续回归。

### 阶段 20 结果：装备预览和换装保存

- 装备／商店左侧复用真实人物、枪械和盔甲模型，固定正面取景；模型按实际装备配置缓存，切换双枪槽或换装后重建。进入装备页自动选中当前穿戴条目。
- `out/stage20-final-menu.log` 退出 0：购买、炼化、双枪装配、两波实战、道具连续购买及保存全部通过。另通过真实按钮把原完美配置的 Infinity Helmet 换成 Combat Helmet，核对下一帧模型和保存／重读。
- 换装首轮失败是检查错误地期待 core:0；原商品引用为 pack4:0，画面和存档正确。修正证据对应的断言后通过，未改变商品配置。
- 正面截图 `out/game-menu-items-check.png`、`out/original-menu-armor.png`、`out/original-menu-equipped-combat.png` 已目视核对。菜单布局仍是宿主适配，不冒充原 Flash 界面。
- 阶段 19 独立版本 Prime 全 500 波结束，28,788 生成／击杀、存活 0、无效生成 0、失败 0，退出码 0。日志、截图、退出码：`out/stage19-pack2-500.*`；Ceres 2 同版本仍在运行。

### 阶段 21 方案：可重复的静音分阶段回归

- 新增根目录 PowerShell 检查入口，直接调用永久 EXE 检查参数，不复制游戏断言。分为核心闭环、六条战役烟测、四图长跑，逐项执行，意外失败停止。
- 保存每项实际命令、退出码、耗时和 JSON 汇总；唯一已知 PROP 43 数据错误必须匹配完整诊断，明确标为 KnownDataIssue，不伪装成干净通过。
- 比较运行前后原 `saves/` 与正式 `userdata/` 的路径和 SHA256；游戏检查只能使用 `out/` 测试账户。
- 使用最新 Release／Debug 实跑核心阶段验证脚本自身，避免交付未经执行的自动化入口。

### 阶段 21 首轮结果

- `out/validation/20260908-070854-110-Debug-Core/`：18 项完成、退出 0；17 项通过、PROP 43 一项 KnownDataIssue，受保护存档变化 0。首次完整 Debug 核心检查覆盖全部新增游戏功能及装备菜单。
- Ceres 2 的阶段 19 独立版本 500 波完成：31,805 生成、31,129 击杀、存活 2（原地图炮塔）、无效生成 0、检查失败 0、退出 0。日志、截图和退出码 `out/stage19-pack9-500.*` 已保留。

### 阶段 22 方案：关卡音效 native 20

- 原 `CLevel::FunctionResolver`（:117599 附近）明确读取 SoundEffect，再由 CSoundQueue 播放 WAV。宿主目前把该调用记为未实现。
- 通过关卡世界接口转至既有声音资源解析／静音层；不能直接把 SoundEffect 编号当 WAV 编号。
- 关卡流测试校验声音引用，真实场景逐条调用当前关卡音效并核对声音计数，再跑波次／战役及 Release 核心回归。常规测试仍保持静音，不声称已经听感验收。

### 阶段 22 结果：声音引用与闭环回归

- `out/stage22-level-flow.log`：19 个有脚本关卡全部通过，原脚本实际发出的 108 次声音引用验证成功；Prime 真实场景两条声音引用／两波检查失败 0。
- Release 核心 18 项：`out/validation/20260908-071720-709-Release-Core/`，17 通过、1 已知 PROP 数据问题、退出 0；六条战役烟测：`out/validation/20260908-071838-806-Release-Campaign/` 全部退出 0。两阶段受保护存档变化 0。
- 原注释迁移核对发现两处成长表注释曾在重构时遗漏，已原样补回。对全部修改文件提取原注释并在当前源码中匹配，未匹配 0；`git diff --check` 退出 0。

### 阶段 23 方案：原关卡缩放曲线

- native 14 是 `CCamera::SetScale`（:117554），目前固定使用初始 0.8，导致波次脚本后续拉远／拉近均失效。
- 核对 `CCamera::GetScale`（:64660）、`SetScale`（:64923）、`UpdatePlayerCamera`（:65255）：一秒进度、余弦插值、途中改目标从当前插值重新开始。新增原名缩放子集，保留既有宿主跟随和视口适配。
- 世界碰撞／人物尺寸仍沿用已验证的初始坐标基准；缩放只乘整体投影，瞄准坐标和战役邻近刷怪的可见范围同步换算。
- 先测端点／中点／重定向／大步长／native 路由，再核对多地图普通波与结尾截图并回归战役。原相机震动和完整位置缓动仍单独记录，不能假称全部相机已移植。

### 阶段 23 已验证结果

- `out/stage23-level-flow.log`：原相机曲线的 0／250／500ms、中途改目标、5000ms 截断、native 14 和关卡重置全部通过；19 个脚本关卡通过。
- `out/stage23-prime-opening.png` 普通新手开场与 `out/stage23-yeroc-finale.png` 末两波至 500 已核对。后者 628 生成／627 击杀，剩余一条原地图植物对象，失败 0。
- 最新 Release 核心 18 项 `out/validation/20260908-072807-532-Release-Core/` 退出 0；17 通过、1 已知 PROP 数据问题。缩放后六条战役烟测 `out/validation/20260908-072957-644-Release-Campaign/` 全部退出 0。受保护存档变化均 0。
- 鼠标路径已核对按 drawable 像素读取并除以当前 camera.zoom，投影与邻近刷怪范围共享当前缩放。没有使用操作系统鼠标自动化，不把代码核对说成人工手感验收。
- 最新 Debug 构建 `out/stage23-debug-build.log` 退出 0，完整核心回归正在运行。

### 交付核对

- 最终 Debug 核心 `out/validation/20260908-073041-405-Debug-Core/`：18 项完成，17 通过、1 已知 PROP 数据问题，退出 0，受保护存档变化 0。Release／Debug 均包含关卡音效和相机缩放。
- 缩放后的 Ceres 2 末两波至 500：`out/stage23-ceres-finale.log/png`，664 生成／642 击杀／保留两条地图炮塔，失败 0；与 Yeroc Sina 末波、Prime 开场截图一并目视核对。
- 原存档完整性基线实际包含 21 个源文件（其中检查器解释 20 份数据记录）；最终 SHA256 变化 0。`userdata/profile.dat` 和 `userdata/original-profile.dat` 均未被自动检查创建，首次正式游戏由用户开始。
- 常规测试全部静音；声音数据解析通过，但未进行外放听感验收。图形、按钮输入和脚本检查不等同于操作系统鼠标或普通玩家难度体验。
- 启动入口：`play-muted.cmd`／`play.cmd`／`play-original-save.cmd`／`research.cmd`；研究菜单 1–38 全部保留，正式入口 26，原存档研究入口 37。
- 当前验收指南 `docs/acceptance.md`；未解／废案边界 `docs/unresolved-behavior.md`；原名对照 `docs/original-name-map.md`。`agent.md` 保留用户指定入口并指向统一约定 `AGENTS.md`。
- 四图 500 波覆盖来自已注明的阶段 16／19 独立版本，最终版本另有完整核心、六战役烟测及两图末波检查。没有把早期长跑冒称为最终二进制的四图完整长跑。

### 阶段 24：启动帮助出口

- 最终启动器检查发现 `--help` 只进入未知参数分支，显示帮助却退出 1。补 `--help`／`-h` 成功退出，仍保留真正未知参数退出 1。
- 范围限于命令行分派；验证两种帮助参数、原存档启动器参数透传、未知参数及研究菜单。已有游戏核心逻辑不变，不重复长跑。
- Release／Debug 构建均退出 0；两种帮助参数均退出 0，未知参数均按预期退出 1；原存档启动器帮助和研究菜单 14 均退出 0。没有创建正式或导入账户。
- 最终二进制 SHA256、核心回归目录和检查边界已写入 `out/delivery-state.json`。可玩闭环及交接工作已完成，暂停夜间自动续跑，等待用户验收；后续未解细节保留为独立研究事项。

- 原 Planet 还包含 BOKOR（pack11）及 Unexplored Planet 占位；BOKOR 的特殊 1/999 脚本需单独核对，不能混入四张 50×10 主地图的通过范围。
- 阶段 11 新版首图完整 500 波结束：`out/stage11-prime-500.log`，28,796 生成／击杀、存活 0、失败 0。对应 EXE 副本包含轨迹与常驻粒子，不包含后续阶段 12 控制器；截图 `out/stage11-prime-500.png`。
- 阶段 13 目录检查：61 个 Mission 完整读取、引用验证失败 0，其中 6 条废弃战役、40 条四星球生存、10 条 BOKOR、5 条多人。全部 Mission 的目标引用列表为空，不能据此声称已恢复战役目标文本。
- 永久菜单 34 任务目录、35 战役档案试玩已加入。首轮发现缺少邻近刷怪、触发区、对话回调；入口能加载地图不代表战役流程完成，继续分项接入。
- PC 版约 2.4.0 是用户估计，尚未核对。
- 已知缺脚本敌人目录 15、62 参见 `docs/arena.md`；保留目录和缺失标记。
