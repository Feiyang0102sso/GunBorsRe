# 2026-09-08 游戏验收

## 启动

在项目根目录双击：

| 文件 | 入口 |
|---|---|
| `play-muted.cmd` | 正式游戏，静音；继续本地账户，无账户时进入新手教程 |
| `play.cmd` | 正式游戏，开启声音 |
| `play-original-save.cmd` | 原完美存档的独立研究副本，静音 |
| `research.cmd` | 永久研究菜单，静音，原有 1–46 项保留；47 紧凑星图、48 签到、49 教程、50 性能、51 商店模板试点、52 共用商品卡验收 |
| `store-template-demo.cmd` | 直接进入商店体验 FILTER、GUNS/ARMOR/POWER UPS 商品卡模板；使用 `out/store-template-demo.dat` 隔离账户，静音 |

实际程序为 `bin/x64/Release/gun_bros_re.exe`，仍直接读取本项目的原版 BIG。无参数直接启动游戏，研究菜单只由 `--research` 或脚本进入。启动器先切回资源根目录。EXE 全部模式支持 `--mute`，静音照常解码音乐、音效和视频并运行游戏逻辑。`--profile out/my-test.dat` 可指定隔离账户，`--skip-intro` 跳过启动视频。

正常账户写 `userdata/profile.dat`；原存档研究副本写 `userdata/original-profile.dat`。后者首次从 `saves/` 的原记录导入，随后继续自己的进度。源存档始终只读。自动检查使用 `out/` 的独立账户。

商店最新改动与来源：[共用商品卡模板](store-cards-template.md)。`--store-card-check --mute` 验证三类卡片、真实文本绑定、开合倒放、预览及购买重载；`--store-template-check --mute` 同时覆盖 FILTER。该项不代表人物相机、完整滚动/筛选/优惠状态和 BANK 结算均已复刻。

## 建议验收顺序

商店模板试点已接入正式商店：点击右下角 FILTER，观察上移与回弹，选择 PISTOL/RIFLE，再点击上方 FILTER 收起；文字、按钮及命中区域随当前 Movie 时间更新。专项命令 `--store-template-check --mute` 输出5张动画时刻截图；范围、源码证据和验证记录见 [商店模板试点](store-template-pilot.md)。本次只完成该组件，不能据此认定整个商店已忠实复刻。

1. 启动静音新手游戏，确认原 Glu 视频、标题页。视频可用空格／点击跳过。无存档先选 Percy／Francis，进入原移动射击、免费步枪切换、拾取手雷炸大怪的教程；老账户不强制重做。签到使用本机日期，五天循环；此后七项主导航可操作。
2. WASD 移动、鼠标瞄准、左键射击；Q 切换已装备双枪，1/2 直接选择。G 使用道具、F 切选；红按钮打开战斗商店，支持购买、左右装备与立即使用，蓝按钮切枪。空格／Esc 暂停，暂停列表可滚动，SURRENDER 进入结算。R 重开、C 碰撞叠加及 N/M 属保留研究控制。
3. 确认敌人受击、死亡、拾取物、经验和升级。兄弟跟随、独立射击，死亡后在下一波复活。正常游戏玩家会受伤和死亡。
4. 死亡／投降后先看未满武器熟练度，再看 Overview／Casualties，最后将 Xplodium 免费即时炼化为金币。炼化前主导航隐藏，完成后依次弹出。核对实际击杀类型、熟练度升级价格、重新进入游戏后的保存结果。
5. 原存档研究入口检查 200 级账户、空经验条、原双枪及 Infinity 三件盔甲；新导入同时读取 weapons3 熟练度。STORE 为右侧大人物、横向两行商品、四分类和多选 FILTER；拥有／已装备标记随状态变化，Q 切预览枪。旧装备研究页保留。
6. 研究菜单 18 查看盔甲组合、33 试用道具、35 选择废弃战役。战役对话使用空格继续；战役试玩不改正式账户。
7. 主导航 GAMES → BOKOR / HORDE，选择十档 Horde 起始难度；检查计时、连杀、POINTS 和保存的最好成绩。顶部货币入口可进入原目录货币包／双向兑换，确认页明确是本地模拟，不发生真实支付。
8. 开声音启动时确认菜单曲与战斗曲：原 iOS CBGM 使用菜单 `game_0.mp3`，每次进入战斗依序选择 `1.mp3` 至 `6.mp3` 并循环当前曲目，不是每星球固定一首。OPTIONS 的音乐／音效设置分别保存。

## 当前范围

本轮对照截图的逐阶段证据、失败修复和验证见 [原版复刻四小时记录](ui-fidelity-progress.md)。默认 `gunbros.cfg` 中 IsConnected=0、DebugMode=0；BROS/BRO-OPS 先显示原离线页，LOCAL PREVIEW 可进入可点击的本地预览。配置联网开关不会连接真实服务器。

原图与程序截图可在 [并排验收页](../out/fidelity-review.html) 中比较。当前仍有逐帧动画、部分布局、教程菜单提示、熟练度助攻奖励与复活道具等差异，未宣称完整 1:1。

窗口内直接连续键入：`chm` 增加 5,000 金币和 500 Warbucks；`cht` 推进一天签到；`chd` 切换调试；`chc` 切换网络预览；战斗中 `chi` 无敌、`chh` 回血，`chw` 解锁四星球波次。测试宜使用 `--profile out/my-test.dat`，避免修改自己的正式进度。

首次操作建议：PLAY → STANDARD → 拖动星图并选择星球 → REVOLUTION → 两行 WAVE → PLAY。BOKOR 为十项 HORDE 后直接 PLAY。LIVE／VS 可看本地选择界面，远端匹配和多人同步未实现。

- 正式四星球一图生存：Prime / Haven / Ceres 2 / Yeroc Sina，原波次脚本 50×10，进度、装备、购买、炼化和本地保存已贯通。
- 原盔甲 233 条、模型 160 个；三种持枪姿态合计 699 组合检查。双枪使用原武器脚本；原敌人控制器已接入电击和冰冻。
- 关卡原音效、音乐和 Glu 视频直接读取；镜头已接原脚本一秒余弦缩放／位置插值、固定目标、跟随、边界与 40 ms 震动切换。随机数序列仍为宿主实现。
- 20 个道具模板、35 个商店引用均可研究。已接入三种回血、护盾、移动／防御／攻击强化、90 秒自动瞄准、普通／电击／冰冻手雷、投放炮塔、三档原时间轴空袭及旧 Tantrum 的原脚本／特效。自动瞄准仍需按住射击；炮塔一次只能投放一座，抛出才扣库存。HUD 显示活动强化与剩余秒数；四个无实际动作的旧道具仍隔离。
- 六条战役档案能加载原地图并实际战斗；触发区、邻近刷怪、导航锁和原文字对话已部分接入。**尚未验证任何一条完整通关**。
- 原存档 20 文件结构、原 CRC 与已解释字段可在菜单 36 导出检查；导入包含等级、货币、装备、库存、四星球逐波完美状态和 weapons3 熟练度。原炼化队列、社交、成就等没有转换；已经存在的导入账户不会被自动覆盖。

## 仍需研究／请补充的细节

- 战役救援／电影／标签和跨 LEVEL 转场未完成；flow 版本未知，仅供参考。缺少实现不能直接判定原版废案。
- BOKOR 已有独立选择页、原关卡调度、计时和得分保存，不占用四星球 50×10 进度。真实多人／好友服务未接；联网入口已有可点击的本地下级页面。
- 默认兄弟仅穿原默认盔甲，携带 Whippersnappers 手枪及 ER97E Elite 步枪，不再复制玩家高等级装备。在线好友配置未恢复。
- 原菜单使用 CMovie 自有时间轴，旧文档的“Flash UI”描述不准确。已恢复原图集、字体和主要布局／提示；部分页面排布、按钮尺寸、活动规则与战役对话时长仍为宿主适配，不能声称像素级一致。
- 旧 Tantrum 在当前 iOS 伤害路径中没有读取其遗留组合倍率，按当前证据播放原 21 秒效果，不自行套用其他版本的倍率。复活等无动作旧道具、弹体完整绘制分组及随机旋转仍待核。
- 敌人目录 15/62 原引用无脚本，保留标记。pack9 PROP 43 记录尾部畸形，地图未引用；菜单 31 会明确报告这个已知错误，不能把它抹成全部通过。

各阶段命令、日志、截图和证据边界见 [整夜进度记录](overnight-progress.md)。自动试跑使用移动／开火输入及真实战斗，但设为无敌；不等同于普通玩家的难度体验测试。

本次 UI 与媒体结果见 [UI 重建记录](ui-reconstruction.md)。旧 PC 参考是用户确认的 2.3–2.4 间泄露开发版，Jungle 未完成；其错误不作为当前 iOS 正式流程的判定依据。本次桌面自动输入在用户物理 Esc 后停止，后续以程序内回归及生成截图验证，完整人工游玩留待验收。

废案、资源问题和未实现机制分列于 [未解行为](unresolved-behavior.md)，原类名及宿主边界见 [命名对照](original-name-map.md)。

## 重复检查

已构建的版本可运行 `./test-muted.ps1 -Configuration Release -Phase Core`，逐项检查核心数据、战斗、菜单、保存和渲染。`Debug` 可替换 `Release`；每项保存命令、退出码和耗时到 `out/validation/`。

`-Phase Campaign` 跑六条战役的移动／战斗烟测，仍不代表完整通关；`-Phase LongRun` 顺序跑四图各 500 波，耗时较长。所有阶段强制静音、意外失败即停止，并比较前后原存档及正式账户 SHA256。已知 PROP 43 必须匹配具体诊断，汇总中保留 `KnownDataIssue`，不标为干净通过。

`-Phase UI` 检查完整媒体解码、视频播放、175 个电影／13 套字体、core 画廊、HUD、菜单以及 BOKOR 首尾难度。报告位于 `out/validation/`。

`-Phase Boundary` 从四图第 499 波分别推进至第 500 波完成，检查最终过波、结束状态及未实现脚本计数。它补充完整长跑，不能替代从第一波开始的 500 波验收。`-ExecutablePath` 可指定独立 QA 构建；各次报告记录实际 EXE。
