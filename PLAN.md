# Gun Bros 重写计划

> 血条位置补充修复：世界锚点投影后再按屏幕半宽居中，9 组分辨率/镜头倍率水平误差由最大 96 像素降至 0，见[对齐回归记录](docs/healthbar-alignment-2026-09-09.md)。

> 最新纠错：按 BIG 敌人脚本复核血条显隐，纠正特殊波次标志与屏幕倍率；合并同帧重复 WAV 并复用音频设备/播放流。原版精确去重窗口仍未证实，宿主适配与原依据分开记录，见[血条与音效修复](docs/audio-health-fix-2026-09-09.md)。

> 2026-09-09 可玩版本优化：单窗口、地图机关、原对话、快捷键及依赖预加载，见 [实现与验收记录](docs/play-optimization-2026-09-09.md)。持续卡顿仍待复现，未将本轮预加载改进等同于完整性能问题解决。

> 同日后续修复：商店两把枪同时显示 EQUIPPED，装备入口按原 `SetGun` 防止重复覆盖，见 [双枪回归记录](docs/dual-weapon-fix-2026-09-09.md)。

> 同日战斗反馈修复：尖塔层级与原脚本计时、切枪动画与按钮、取消 F/R、结算继续更新、敌人血条及穿透弹碰撞；专项和四星球/Horde 各两波回归通过，见 [战斗修复记录](docs/combat-feedback-fix-2026-09-09.md)。

## 当前下一阶段：清理硬编码，按原资源与源码复刻（UI 与游戏逻辑）

本节是用户最新指定的下一阶段工作入口，优先于下方历史阶段中的占位、截图近似和离线模拟方案。历史实现与验收记录保留，但“已经接入”不代表本节任务已经完成。以下全部从未完成状态开始，不能因已有解析器或 BT 而提前勾选。

### 执行与勾选规则

- 按“核对 BIG/存档原始字节 → 查 BT 与原源码读取、消费函数 → 实现 → 验证 → 更新本节”的顺序推进。资源数据运行时从 `big/` 读取，禁止手写替代，也禁止搬进手工 JSON/CSV。
- 分清原资源数据、原代码常量、宿主适配、研究/测试固定值和未实现逻辑。原版算法与常量有证据时保留在代码中；不能把所有数字机械地改成资源读取。
- **完成一个任务，验证通过后立即将对应 `[ ]` 改为 `[x]`。** 同时在该项后补完成日期、实现位置及验证记录链接；记录原资源定位、BT、原类/函数/反编译行号、测试命令和结果。只完成部分时保持未勾选，注明剩余内容；较大任务继续拆成独立复选项。
- 找不到依据的字段或行为保持未完成，记录已查证据与阻塞原因，不造数、不静默回退。下列源码行号是本次审查定位，后续变动以函数名重新定位。
- 每阶段验收相关实际流程，常规运行和自动验证使用 `--mute`，保留已有里程碑入口。成功构建、能打开页面或解析到文件末尾，均不能单独作为完成依据。

### A. 建立来源清单与清理边界

- [ ] **A1 全仓补查硬编码与替代逻辑。** 当前发现不是穷尽清单；检查 `src/gun_bros_re/runtime/`、`gun_bros/`、`engine/` 及共享里程碑代码，列出实际进入正式流程的手写表、按 ID 分支、默认值、固定计时和简化算法。每项分类为“需读取原资源 / 需按原算法重写 / 有原依据可保留 / 仅研究测试 / 未知”，并将新发现补入本节。
- [ ] **A2 建立资源到消费函数的可追溯关系。** 为本阶段涉及的类型记录包 hash、类型、ordinal、字段偏移、原值、BT、原消费类/函数及重建调用位置；核对包索引、资源编号和逻辑 ID 的区别，纠正把 STORE 商品条目当 CGun 武器模板等混淆。

### B. UI：从原资源组装完整界面与动画

- [ ] **UI1 菜单数据来源。** 审计 `runtime/OriginalMenuData.h`、`OriginalMenuData.inc`、`MovieNames.inc` 和 `GameFrontEnd.cpp` 中的表；区分原程序固有的绑定枚举与从资源复制或自行填写的数据，后者改为从 BIG 解析。验收每个绑定都能追溯至原资源或原菜单函数，不遗留第二套手写资源事实来源。
- [ ] **UI2 Movie 与 Sprite 公共执行链。** 按 `CMovie`、Sprite 和原菜单代码实现章节、区域、锚点、关键帧、时间插值、循环、显隐、层级、裁剪、文本/图片绑定、输入命中和完成事件；覆盖 `runtime/MovieRenderer` 及其调用方，移除按截图填坐标或用固定计时模拟资源动画的路径。
- [ ] **UI3 星图、模式选择和选关。** 按 Planet、Mission、Level 条目及 Movie/Sprite 引用组装，追踪星球位置、图标、轮次、波数、要求、解锁和进度显示的原消费逻辑；清理 `GameFrontEnd.cpp` 的手写位置/深度表。验收切换星球与轮次、选择起始波、进入正确关卡及返回后的状态刷新。
- [ ] **UI4 商店、装备和升级。** 按 STORE、CGun、Armor、Powerup、Bank 等实际条目及菜单绑定读取价格、属性、顺序、隐藏条件、分类、说明、模型和动画；恢复购买、装备、熟练度升级与库存刷新的原调用链。验收多类商品、不可购买/已拥有/已装备状态及存档重载。
- [x] **UI4.1 商店 FILTER 模板试点。** 2026-09-08 完成。将静态开闭时间替换为 BIG 的 SORT_BAR 章节；按原菜单绑定独立文字区域，让底板、选项和命中区域随正放/倒放一起更新。实现：`CMovie::GetChapterRange`、`GameFrontEnd.cpp` 的 `AdvanceStoreFilter/DrawStore`；专项、菜单回归及175个Movie检查均退出0，23个受保护文件无变化。中途反向、多选、不提前命中、不误购已验证，保留5张截图和研究入口51。证据见 [商店模板试点](docs/store-template-pilot.md)；此子项不代表 UI4 全部完成。
- [x] **UI4.2 GUNS 与共用商品卡模板绑定。** 2026-09-08 完成。按 `CMenuStoreOption` 恢复五种字体控制码、STORE 属性/说明、原尺寸缩略图、真实分类/价格、章节开合与熟练度子 Movie；同链路覆盖 ARMOR、POWER UPS，修正分类高亮、数量角标及消耗品拥有状态。Release 构建、772条真实文本/资源变异/三类开合专项、展开预览与购买重载、菜单回归、175个 Movie 检查均退出0；保留18张商品卡截图及研究入口52，23个受保护文件无变化。实现与原函数/BT路径见 [共用商品卡模板](docs/store-cards-template.md)。UI4 其余部分保持未完成。
- [x] **UI4.3 商店人物原版投影与裁剪。** 按 CMenuStore region2 → CMenuMeshPlayer → CBrother::DrawUI / CMeshCamera::OrientForUI 使用活动躯干原 bounds，取消把region当scissor/viewport的错误。实际GL检查先失败再通过；两位兄弟×七类枪与区域变异、三类商品卡回归均退出0。详见[本轮记录](docs/ui-original-overnight-2026-09-09.md)。独立旧装备研究页相机不在本项范围。
- [x] **UI4.4 升级弹窗主/星条状态与原绑定。** 原六态、章节边界、1×星条、250ms原生闪光、满级自动关闭；NEXT百分比与原字体/MDS按钮/区域。入口53和--upgrade-popup-check覆盖13阶段、资源章节变异、重复点击、重开及三级升级保存重载；175个Movie回归退出0。不据此勾选整个UI4。
- [x] **UI4.5 升级不足币、换枪和卡内熟练度。** 原两按钮动态总价/差额提示、最小足额BIG内购、等待后重试升级、切换另一把未满级装备枪；原账户隔离回归及重载退出0。卡内Movie91使用原99比例和固定星Sprite初帧，210枪阈值及章节变异通过。2026-09-09证据见[本轮记录](docs/ui-original-overnight-2026-09-09.md)。普通商店三按钮不足币提示仍待恢复。
- [x] **UI4.6 Bank原商品卡与币种交易。** 原MDS动作提取纠偏，原Movie卡片/货币筛选、IAP与兑换提示；25阶段覆盖全部16商品、第二行点击、双向兑换、不足币与存档重载，整体菜单/实战回归退出0。离线4秒到账为UI_sample既有用户授权，金额全读BIG；完整按钮动画及筛选实际逐项点击仍需补验。
- [ ] **UI5 精炼、签到、奖励和社交相关页面。** 核对对应配置、条目、存档客户端和菜单函数，替换手写布局与虚构状态；本地研究模拟须与正式状态分离，不能以假奖励或假联网结果填补远端服务缺失。验收可由本地证据支持的完整流程，明确无法恢复的服务边界。
- [ ] **UI6 战斗 HUD、教程、结算和设置。** 按原 Movie、字符串及状态绑定恢复血量、波次、奖励、提示、暂停、死亡/重开、升级结算及设置反馈，检查动态刷新与开闭动画；原资源中的参数不得被宿主显示常量覆盖。
- [ ] **UI7 全页面查漏与联动验收。** 从原菜单类、Movie 目录和现有入口清点 UI1–UI6 未覆盖的弹窗、加载页及子页面，拆项补齐；逐页验证进入、退出、输入、动画完成事件和数据更新，保留截图与来源记录，不能只验收静态外观。

### C. 非 UI：已发现的硬编码和简化实现

- [ ] **G1 活动目标与实际发奖。** 替换 `CProfileManager::ActivityTarget/ActivityProgress/ClaimActivity`（`gun_bros/CProfileManager.cpp:332` 起）的8项自创活动、金币和战争钞票表；追踪原挑战/奖品条目、进度客户端和领取逻辑。验收目标、累计进度、领取条件、防重复及货币保存；若属于服务端数据缺失则记录边界，不把自创奖励冒充原版。
- [ ] **G2 星球到地图/关卡的关联。** 清理 `runtime/GameFrontEnd.cpp:38` 的 `kPlanetPacks`、`kPlanetMaps` 及正式入口中的固定包/地图选择，沿原资源引用和原菜单逻辑选择 Mission/Level/Map。验收所有可进入星球、教程及 BOKOR 的资源选择与进度续玩；原源码确实固定的关联需先举证再保留。
- [ ] **G3 生存 LEVEL 选择。** 移除 `SurvivalSession::Load`（`runtime/SurvivalSession.cpp:31`）以“脚本状态数大于100”识别生存关卡的启发式条件，恢复原版关卡选择链。验收不会因状态数量变化选错或漏选，也不会把历史战役误当正式生存关卡。
- [ ] **G4 刷怪与掉落选点。** 恢复原 `CEnemySpawner` 及路径节点的筛选/评分/随机规则，替换 `SurvivalSession::SpawnEnemy`、`SpawnPickup` 的轮流选点策略；保留地图中的真实节点，核对锁定、启用、距离、占用和无可用节点时的行为。验收四面刷怪、掉落、受限节点和重开流程。
- [ ] **G5 开场、换波和剧情完成时机。** 核对 `SurvivalSession::Restart/Update/UpdateDialog` 的1200毫秒过场、阅读时长公式及其他计时来源；由原 Movie 完成事件、脚本或原生逻辑决定流程推进。不能批量删除已有原资源依据的时长；验收不提前刷怪、不漏发或重复发完成事件。
- [ ] **G6 玩家移动计算。** 核对 `CombatScene::Update` 的基础速度220及整条倍率链，对照原 `CPlayer::UpdateMovement`（反编译 `:101384`），恢复输入幅度分段、武器参数、熟练度及其他有依据的倍率；Windows 输入适配与原移动公式分离。验收不同装备、输入强度和状态下的位移与动作。
- [ ] **G7 弹体速度、射线长度及特效常量。** 核对 `gun_bros/WeaponEffects.cpp:18` 的基础速度450、最大射线长度1400和相关固定参数，区分原代码常量、模板/Flow 参数与临时上限；结合 `CGun`、`CBullet` 原函数修正。验收弹速、射程、碰撞截断、持续发射和不同弹体类型，保留正确的原常量及其来源。
- [ ] **G8 AI 跟随、瞄准与随机机制。** 核对 `CBrotherAI` 的130/100跟随距离、跟随速度、反应时间和瞄准误差；替换正式流程中 `0xB6400`、`0xA170` 等固定宿主随机种子及不对应原版的随机调用机制。已找到原构造函数依据的200/220瞄准距离不当作错误删除；固定种子的可重复实验留在测试入口。验收跟随、选敌、换枪、失去目标和重开行为。
- [ ] **G9 存档、初始状态和经济逻辑补查。** 审计 `CProfileManager`、`CPlayerConfiguration`、`OriginalProfile`、签到、精炼和库存实现，对照 `saves/`、存档 BT 及原客户端读写/消费函数；区分当前自有宿主存档格式与原存档语义，核对初始装备、货币、解锁、进度、领取标记及迁移行为。发现替代规则后拆项修复，原存档只读，使用副本验收读写一致性和重启后的状态。

### D. 本阶段完成验收

- [ ] **D1 清理遗留替代路径。** 对照 A1 清单检查正式调用链，确认已完成项没有旁路手写表、静默 fallback 或按 ID 补丁；有依据保留的常量写明原类/函数、用途与单位，未知项继续列为未完成。
- [ ] **D2 完整游戏回归与交接。** 覆盖菜单选关、装备购买/升级、移动射击、刷怪、掉落、受伤死亡、重开、换波/轮次、结算和保存重载；记录命令、退出码及必要截图，更新 `docs/acceptance.md` 和对应模块文档。只有上述实现任务全部完成且通过相关验收，才结束本阶段。

### 本阶段资料入口

- 约束与来源优先级：[`AGENTS.md`](AGENTS.md)。原资源：`big/`；研究样本：`big_360_out/`；原存档：`saves/`。
- 格式及字段证据：[`docs/binary-template-catalog.md`](docs/binary-template-catalog.md)、[`docs/entry-field-semantics.md`](docs/entry-field-semantics.md)、[`docs/ui-binary-templates.md`](docs/ui-binary-templates.md)、[`docs/save-binary-templates.md`](docs/save-binary-templates.md)、[`docs/flow-bytecode-reading.md`](docs/flow-bytecode-reading.md)。
- Binary Template：`_Big_tool/binary template/big_assets/`、其下 `entries/` 与 `maps/`、`_Big_tool/binary template/saves/`；BT用于辅助理解，不以010 Editor是否可执行作为本阶段完成条件。
- 原行为：`gunbros`、`_IDA_OUT/gunbros_3.6.0_IOS.c`、`_IDA_OUT/source_tree.md`；定位辅助：`out/binary-research/source-index.json`、`out/binary-research/flow-disassembly/`。
- 现有模块和验证边界：`docs/player-weapon.md`、`docs/armor.md`、`docs/arena.md`、`docs/ui-reconstruction.md`、`docs/ui-fidelity-progress.md`、`docs/binary-research-progress.md`、`docs/acceptance.md`。

---

## 历史计划与阶段记录

> 2026-09-08 四小时截图复刻：导航／商店／星图选波、签到 cfg、教程、战斗商店、熟练度结算、炼化、离线社交页及设置已接入。实测与未完成项见 [本轮复刻记录](docs/ui-fidelity-progress.md)，验收以最新记录为准；未声称达到逐像素、全行为 1:1。

> 2026-09-08 当前可玩入口、操作和边界见 [游戏验收](docs/acceptance.md)，本轮逆向证据与分阶段验证见 [整夜进度记录](docs/overnight-progress.md)。以下保留原重写计划，历史里程碑继续可用。

> 本日上课期间追加授权已执行：默认进入游戏、原 Glu 视频及 MP3、CMovie 菜单与战斗 HUD、默认兄弟装备、离线下级流程、BOKOR。详细方案与验证见 [UI 重建](docs/ui-reconstruction.md)。下列历史非目标中的联网限制不再排除本地模拟页面；真实远端服务及多人同步仍未实现。

用 C++ 在 Windows 上重写 Glu Mobile 的自研引擎，**运行时直接读原版 `.big`**，让游戏跑起来。

参考资料：
- [`_IDA_OUT/gunbros_3.6.0_IOS.c`](_IDA_OUT/gunbros_3.6.0_IOS.c) — 反编译产物，**唯一真相来源**
- [`_IDA_OUT/source_tree.md`](_IDA_OUT/source_tree.md) — 还原出的原始源码树 + P0~P3 优先级
- [`_Big_tool/`](_Big_tool/) — `.big` 格式逆向笔记，**仅供参考，不保证正确**（本地文件，不入库）
- [`_IDA_OUT/CVector.inl`](_IDA_OUT/CVector.inl) — 误打包进 IPA 的一手引擎源码

---

## 一、目标与非目标

**目标**：行为对得上原版，能玩。

**非目标**（明确排除，避免中途摇摆）：

- 不做二进制级等价复刻，不追求逐函数一致
- 不解包资源，不做资源转换管线
- 不做联网、多人、广告、内购、成就、社交
- 不支持 hvga，只跑 xga
- 暂不跨平台，先 Windows 跑通

---

## 二、技术栈

| 项 | 选择 |
|---|---|
| 语言 | C++17 |
| 窗口 / 输入 / 音频 | SDL3 |
| 渲染 | OpenGL 3.3 Core，加载器自己写（~30 个函数，`engine/platform/GLLoader.h`） |
| 图像 | PNG 编解码自己写（`engine/CPNG.cpp`），不用 stb_image |
| 解压 | zlib（`inflate` 给资源和 PNG，`deflate` 只给截图） |
| 视频 / MP3 | Windows Media Foundation 系统解码器，SDL3 播放 PCM；无额外 ffmpeg 运行依赖 |
| 构建 | Visual Studio / MSBuild（`gun_bro_re.slnx`，工具集 v145），只支持 x64 |
| 第三方库 | 只有 zlib（源码内置）和 SDL3（官方预编译包），不用包管理器 |

**glad 和 stb_image 最终都没用**（M2 时决定）。原因是成本账反了：我们只会用到几十个 GL
函数，glad 生成的是 4000+ 行；而全部 1534 张 PNG 只有 3 种变体（8-bit，非隔行，
colortype 6/2/3），自己写解码器约 180 行，且出问题能直接调。引入外部代码要省的那
260 行，抵不上多出来的 4300 行怀疑对象。

**SDL3 用官方预编译 VC 包**（`src/third_party/SDL3/`，include + x64 的 lib/dll），
而不是源码内置——源码内置意味着往 vcxproj 里塞几千个文件，或者被迫引入 CMake，
两条路都和"简洁"背道而驰。

### 关键决策与理由

**为什么 C++ 而不是 C#** — 决定性因素是**能否推迟理解**。开发模式是对着 IDA 伪代码逐函数啃，而伪代码里全是 `*(_DWORD *)(v30 + 544)` 这种指针算术。C++ 里可以原样粘贴、先跑起来、之后再查清 offset 544 是什么字段；C# 里必须先搞懂对象布局才能写出第一行能编译的代码。这个成本要乘以 3 万个函数。次要理由：`CVector.inl` 是 C++ 模板源码；引擎对象模型是 `CClass` + 虚表 + 多继承 thunk，C++ 原生映射。

**为什么直接用 VS 工程而不是 CMake** — 只跑 Windows、只有一个可执行目标、第三方库全部源码内置。CMake 的三个卖点（跨平台、依赖发现、多生成器）在这里一条都用不上，留着只是在 IDE 和编译器中间多垫一层间接。工程交给 VS 自己维护，加文件右键就完事，不用记 `add_executable` 的语法。代价是日后要上 Linux 得重新配一套——`_IDA_OUT` 里的东西还够啃一年，那天很远。

**为什么第三方库源码内置而不是 vcpkg** — 依赖只有 zlib 一个，而且只用 `uncompress` 一个函数。为它装一整套包管理器、维护 manifest、在每台机器上先跑 bootstrap，成本远大于把 15 个 .c 文件拖进工程。等 SDL3 进来时再重新评估。

> M2 时已重新评估：SDL3 用官方预编译包，只有 include/ + lib/x64/ 两样东西，
> 结论不变——仍然不需要包管理器。这批二进制入库，见第三节版本控制。

**为什么 GL 3.3 Core 而不是 bgfx / SDL_GPU** — 原引擎的渲染能力集小到极点（纯色 / 纯纹理 / 纹理×顶点色，一个 mvp 矩阵，alpha blend + alpha test + 深度测试，无纹理压缩、无光照、无后处理），任何现代 API 都绰绰有余，选型依据只能是**翻译成本**。GL 3.3 与 ES2.0 是同一心智模型，`CGraphics_OGLES2` 的每个方法基本一一对应，原 shader 改几个关键字就能用。多一层框架抽象就多一个怀疑对象，会干扰"对着反编译逐行验证"。

---

## 三、目录结构

```
gun_bro_re/
├── gun_bro_re.slnx            解决方案（VS 新版 XML 格式）
├── gun_bro_re.vcxproj         唯一的工程，源文件列表都在这
├── gun_bro_re.vcxproj.filters 解决方案资源管理器里的目录树
├── .gitignore
├── PLAN.md
├── src/
│   ├── gun_bros_re/
│   │   ├── main.cpp      ← 只做参数解析和里程碑分发
│   │   ├── engine/       ← platform/shared 保留的 ~95 个
│   │   │   └── platform/ ← SDL3 窗口 / GL 上下文 / GL 加载器
│   │   │                   （原 platform/shared/cocoa 的角色）
│   │   ├── gun_bros/     ← src/gunbros 保留的 ~160 个
│   │   ├── sprite_glu/   ← src/spriteGlu3 的 3 个，与 gunbros 平级的独立子系统
│   │   ├── milestones/   ← 每个里程碑一个验收台，main 保持薄
│   │   └── shaders/      ← 7 个，从反编译里的 GLSL 原文改写为 GL 3.3
│   └── third_party/      ← zlib（源码）/ SDL3（预编译）
│
├── big/                  26 个 .big + packTOC（运行时直接读，不解包）
├── png/  mp3/  glu_logo/ 散资源
├── gunbros               原 ARMv6/v7 FAT 二进制
├── _IDA_OUT/  _Big_tool/ 逆向资料（CVector.inl 在 _IDA_OUT/ 下）
└── _junk/                iOS 打包残留，暂不删

bin/  obj/                构建输出，按 $(Platform)\$(Configuration) 分开，不入库
```

**资源路径**：工程注入 `ASSET_ROOT` 宏，开发阶段用绝对路径。不搞资源打包和路径搜索。

`ProjectDir` 形如 `E:\...\gun_bro_re\`，先把反斜杠换成正斜杠、去掉结尾斜杠，
才能直接拼进 C++ 字符串字面量（反斜杠在里面是转义符）：

```xml
<PropertyGroup Label="UserMacros">
  <AssetRoot>$(ProjectDir.Replace('\','/').TrimEnd('/'))</AssetRoot>
</PropertyGroup>

<ItemDefinitionGroup>
  <ClCompile>
    <PreprocessorDefinitions>ASSET_ROOT="$(AssetRoot)";%(PreprocessorDefinitions)</PreprocessorDefinitions>
  </ClCompile>
</ItemDefinitionGroup>
```

**版本控制**：原版资源（`big/ png/ mp3/ glu_logo/ gunbros _junk/`）是 Glu Mobile 的版权素材，
体积也有 220M，一律不入库。逆向资料里 **`_IDA_OUT/` 入库，`_Big_tool/` 不入库**——
后者是本地笔记和一次性脚本，结论一旦确认就应该搬进代码注释或 PLAN，
留在库外反而少一处需要同步的真相。

> M1 对 `.big` 格式的校正记录写在 `_Big_tool/refs.md` 和 `tocs.md` 里，只存在于本地。
> 关键结论（handle 位域、聚合寻址、包哈希算法）已经落到
> `CBigFileReader.h` 和 `CAggregateResource.h` 的注释里，不依赖那两份笔记。

**第三方二进制**：`src/third_party/SDL3/lib/x64/` 的 `.lib` 和 `.dll` **入库**，
否则 clone 下来链接不了。`.gitignore` 里为此开了例外——`x64/` 和 `*.lib` 两条规则
本来会吞掉它们，而 git 不允许在父目录被排除的情况下恢复其中的文件，
所以要先恢复目录再恢复文件。

---

## 四、里程碑

每个里程碑都有**能看到的验收结果**，做不出来不算完成。

### M0 — 骨架

VS 工程 + SDL3 窗口 + glad 加载 + 主循环。

> **验收**：能开关的黑窗口，稳定 60fps，`glGetString(GL_VERSION)` 打出 3.3。

> 实测：`GL 3.3.0 NVIDIA 546.92`，开 vsync 后稳定 165fps（锁在显示器刷新率）。
> M0 与 M2 合并实现——单独的黑窗口没有信息量。

### M1 — 读得到资源 ★第一块地基

照反编译实现容器解析：

| 文件 | 原位置 |
|---|---|
| `CBigFileReader` | `platform/shared/components/` |
| `CResourceBigFile` | `platform/shared/systems/` |
| `resPackTOC` / `resTOCManager` | `src/gunbros/` |

链路：`packTOC_xga.dat`（**大端**）→ `FGIB` 头 → table1 游程映射 → table2 偏移表 → 资源块头 → zlib 解压。

注意：文件名是 `CResTOCManager::Init` 里逐段拼的（`"p"` + … + `"_"` + artset + `"."`），artset 写死 `"xga"`，`pack0_core` 有特判。

> **验收**：命令行列出 `pack0_core_xga.big` 全部资源的 `id / groupHash / 大小 / 压缩标志`，随机抽 20 个解压后校验魔数（PNG / RIFF）。与 `_Big_tool` 输出交叉比对，**不一致以反编译为准**并记录差异。

### M2 — 看得到贴图

PNG 解码 + 纹理上传 + 7 个 shader 移植 + 最简 2D 批处理（dynamic VBO 每帧重填，先不做架构）。

shader 移植改动：`attribute`→`in`、`varying`→`out`/`in`、`gl_FragColor`→自定义输出、`precision` 限定符去掉、`glAlphaFunc` 用 `discard` 模拟。

**必须原样照抄**：UV 缩放 `0.0002441406255`（= 1/4096）。贴图坐标在美术数据里是整数存的，改了全部错位。

> **验收**：窗口里显示一张从 `.big` 取出的 PNG，尺寸和颜色正确。

> 实测：三种 colortype 各验一张——`pack0_core` 313（512x512 RGB，角色贴图集）、
> 316（136x114 RGBA，武器图标，alpha 混合正确）、`pack1` 467（512x512 调色板，敌人贴图集）。
> UV 缩放常量随之验证：错了图会整体错位。
> `--screenshot <file.png>` 存下首帧后退出，里程碑不需要人盯着也能验收。

### M3 — 画得出地图

`TILESET`(Section 25) + `TILELAYER`(Section 24) + `CLayerTile` 拼图 + 相机。

照抄两个细节：`tileId == 255` 跳过不画；图层比画布小时**行列取模环绕平铺**。图块尺寸恒 256×256，绘制边长取 `tiles[0].w`。

> **验收**：完整显示一关静态地图，鼠标可拖动查看，图块无错位。

> 实测：22 张地图里 20 张正常画出，图块无错位。默认缩放到适应窗口（能一眼看完整关），
> 鼠标拖动平移、滚轮缩放。`--maps` 列出含地图的包，`--map <pack> <n>` 选图。
> 环绕平铺在 pack2 map 4 上验证：12x5 的岩浆层铺满 13x5 画布。
>
> **pack7 的 map 0~4 画出来是空的，这不是缺陷**——它们的图块层数据本身全是 255。
> map 1 和 map 2 已完整解析所有图层仍是 0 个图块，可以确证。这几关的视觉内容全在 PROP 里，
> pack7 有 69 个 PROP 模板，是所有包里最多的。
>
> **缩小查看时图块边缘有淡接缝**，是无 mipmap 的缩小走样，不是拼接错位——
> 1:1 查看时干净。原版同样没有 mipmap，所以保持一致，没有加。

#### M3.1 — 地图上的装饰物

`CLayerObject` + `CProp::Template` + `spriteGlu3` 三件套，把 PROP 画到地形上。

先补齐了 `CMap` 的图层解析：借 [`_Big_tool/binary template/big_assets/maps/map.bt`](_Big_tool/binary%20template/big_assets/maps/map.bt)
（对 83 张地图验证过）拿到 PathLink / PathMesh 的布局，与反编译逐字段核对后写进
`CMap.cpp`。之前遇到路径图层就停，藏在它们后面的对象层根本读不到——补齐后
props 从 1624 涨到 1653。现在每张地图的所有图层都读到资源末字节，剩余 0 字节。

寻址链五级：对象层 → PROP 段基址 + localIndex → `CGameSpriteGluRef`(packHash +
archetype + action + anim) → archetype 资源 → anim → frame → sprite → spriteMap
→ 包级 imageSlot 表 → archetype 的 rect 表 → 图集页 PNG。

> **验收**：完整地图上出现石堆、管道、传送门、岩浆池。

> 实测：22 张地图共 **1653 个 PROP，0 个解析失败，0 个精灵部件画不出**。
> **pack7 的 5 张"空图"现在全都有内容了**，证实了上面的判断。
>
> 查看器把全部 22 张图排成**一条扁平列表**：左右键逐张走，走到一个包的末尾直接进下一个包；
> 上下键整包跳。`--map` 只决定从哪张开始，不再是切包的唯一手段。
> `--maps` 会把这条列表连同全局编号一起列出来。`T` / `P` 开关地形与装饰物。
>
> 顺带修了一个**潜伏的跨包寻址 bug**：图块集和图集原先拿"地图所在的包"去解析，
> 把引用自带的 packHash 丢掉了。照 `CGunBros::GetGameObject` 的做法，
> packHash 是地址的一部分——**先选包，再加段基址**。实测这 69 个引用目前碰巧都指向自己的包，
> 所以画面没错；但 PROP 已经有 5 个跨到 pack1，同样的写法迟早会踩。
> 现在所有引用统一走 `ReadSectionResource(packHash, section, ordinal)`。
>
> 三个细节照抄了原版：
> - **三个绘制槽**。`CProp` 有主 / 前景 / 背景三个 `CSpritePlayer`，261 个模板里
>   有 85 个**只填背景槽**——只画主槽会静默丢掉三分之一的布景。
> - **绘制顺序**。`CRenderQueue::Draw` 是按 `(zOrderGroup, y)` 排一次序、
>   然后整队走三遍（全体背景 → 全体主 → 全体前景），不是每个物件画完自己的三槽。
>   一个物件的前景要压在**下一个**物件的主精灵上。
> - **两级倒序**。`SetFrame` / `NextSprite` 从最后一个部件往前走，所以帧部件和
>   精灵部件都是倒着画的。顺着画的话叠放关系会整体翻过来。
>
> `CQuadBatch` 因此改了两处：分组从"按纹理合并"改成"按纹理游程切分"
> （合并会把后提交的四边形提前画，地面就盖到石头上了），以及每组带自己的混合模式
> ——spriteMap 的第三个字节是混合标志，bit6/bit7 是加法混合，不做的话
> 所有辉光贴图都带一圈黑框。
>
> **未做**：ENEMY（3D mesh）、PARTICLEEFFECT（粒子系统）、PLAYER（不可见标记），
> 合计占物件总数的 7%。sprite 替换表和图元填充矩形也没做——
> 这几个包的替换组数全是 0，图元只有 pack7 有 1 个。
> 动画不播放，每个槽固定取第 0 帧（原版取随机帧，静态查看器要可复现）。

#### M3.2 — 地图活起来

`CSpritePlayer` + 主循环时间步，把 M3.1 画出来的静止布景播起来。

动画数据在 M3.1 就已经解析进 `CSpriteGluArchetype` 了（`AnimationStep` 的帧号与时长），
只是每个槽固定取第 0 帧。这一步补的是那个缺席的时钟。

> **验收**：地图上的岩浆、辉光、传送门动起来，图块与叠放关系不变。

> 实测：22 张图 **1653 个 PROP、0 个未解析、0 个部件画不出**，与 M3.1 逐项一致。
> 其中 **138 个 PROP 会动**（模板里至少一个槽多于一步），pack9 map 0 最多，有 31 个。
> 只有 pack7 map 2 一张图完全静止。
>
> `--advance <ms>` 先把动画时钟推进指定毫秒再画第一帧，这样 `--screenshot` 也能验收动画：
> 同一张图取几个时刻截图对比即可。像素差随时间增长（pack9 map 0 相对 t=0：
> 120ms 差 2156 像素，200ms 差 4304，400ms 差 4422），且差异只落在辉光上，
> 地形与其余布景一动不动。查看器另加 `空格` 暂停、`.` 单步 80ms。
>
> **`AnimationStep` 的字段顺序原本就是对的**。`LoadArcheType`（:57552）里线上先帧号后时长、
> 内存里反过来存成 `{时长, 帧号}`——和 sprite map 那个坑同型，但 M3.1 按线上顺序读的，
> 没踩到。`kDurationUnitMs = 10` 也和原版那个 `10 *` 对得上。
>
> 三个细节照抄了原版：
> - **时间结转**。`AdvanceFrame` 进入新一步时不是把时钟重置成整段时长，而是
>   `max(新时长 + 上一步的欠账, 新时长 / 2)`。欠账让慢帧不会拉长动画，
>   下限让一个极慢帧不会无限跳步。帧率无关性就是从这来的。
> - **只有主槽随机起始帧**。`CProp::Bind`（:124916）对主槽调 `Random(0, 步数-1)`，
>   前景槽和背景槽都从第 0 步开始。不照抄的话一片岩浆会整齐划一地脉动。
>   这里用按绘制序的确定性哈希代替真随机，`--screenshot` 才还能两次跑出同一张图。
> - **单步动画回卷到自己时不动时钟**。原版在那条路径上直接返回，
>   这正是"静止布景保持静止"的实现方式，而不是靠额外判断。
>
> **绘制顺序没有跟着动画变**。排序键 `(zOrderGroup, y)` 只跟 prop 的位置有关，
> 而动画只换贴图不换位置——所以队列仍然只在加载时排一次。zOrderGroup 也仍按第 0 步判定，
> 与 M3.1 逐字节一致。`CRenderQueue` 因此**没有**抽出来：现在没有会移动的实体，
> 抽出来只是多一层没有职责的间接。等 M4a 有了玩家和敌人再说。
>
> 几何改成每帧重建。实测最大的一张图也只有 190 个图块 quad + 342 个精灵 quad，
> 对一个本来就为"每帧重填"设计的 dynamic VBO 来说无关痛痒，所以没有把图块拆出去缓存。

#### M3.3 — 背景流动起来

岩浆、海水、星空的流动，是**图块层整层漂移**，不是换帧。`CLayerTile::Update`（:126770）
维护一对偏移：`offset = frac(offset + speed × dt)`，`SetSpeed`（:126803）把参数乘 −0.05。

> **验收**：有滚动的地图上，背景层持续漂移，地形与装饰物不受影响。

> 实测：22 张图里 **6 张有滚动背景**，其余 16 张原版就是静止的。1653 个 PROP、0 未解析，
> 与 M3.2 逐项一致。
>
> | 地图 | 滚动层与速度（图块/秒） |
> |---|---|
> | pack12 map 0 | 层0 `(0, −0.075)`，层1 `(+0.06, +0.035)` — 双层视差 |
> | pack2 map 1 | 层0 `(−0.025, 0)` |
> | pack2 map 2 | 层0 `(−0.05, 0)` |
> | pack2 map 5 | 层0 `(0, −0.05)` |
> | pack2 map 7 | 层0 `(−0.06, −0.065)` |
> | pack9 map 0 | 层0 `(−0.15, −0.15)` — 最快，对角漂移 |
>
> **速度不在地图数据里**。它由关卡脚本在运行时设置：脚本调 `CLevel::FunctionResolver`
> 的 42 号函数（:117823），后者转调 `CLayerTile::SetSpeed`。原生函数 id 是 16 位，
> 高字节选类（5 = CLevel）、低字节选函数，由 `ScriptResolver::ResolveFunction`（:107633）分派。
>
> 于是这里**没有实现解释器**，而是按字节签名扫描出这些调用——见
> [`LevelScriptScan.h`](src/gun_bros_re/gun_bros/LevelScriptScan.h)。
> 调用是固定的十字节形状（`00 2A 05 03` + 三个 uint16），且这批档案里三个参数全是字面量
> （bit15 标记字面量，`CScriptInterpreter::CallFunction` :107496），所以数值是**读出来的，不是编的**。
> 代价是扫描判不出可达性：写在条件分支里的滚动会被当成无条件。这批档案里没有这种情况
> （命中全在关卡开头的初始化段），但扫描本身证明不了。**M4a 做出 `CScript` 后这个文件应该删掉。**
>
> **偏移只留小数、丢掉整数部分**，所以漂移永远在一个图块内循环。这只有在"整层平移一格看不出区别"时
> 才无缝——也就是均质填充的岩浆、海水、星空，恰好就是唯一被滚动的那几层。
> 结论：**滚动完全不需要移动格子索引，只要平移绘制位置**，环绕平铺（M3 就做了）负责其余部分。
>
> 漂移会在让开的那一侧空出一条不到一格的缝，所以那一侧要多画一列（或一行）。
> 只补**漂移方向对侧**那一边：四边都补的话，整张图会多出一圈可见的边框。
> 这一格在原版里永远看不到（相机不会走出地图），但本查看器是整图适配的，所以它露在地图外沿。

#### M3.4 — 脚本跑起来

照反编译实现 `CScript` + `CScriptInterpreter`，执行档案里的字节码，
把 M3.3 那个字节签名扫描换掉。

**从 M4a 里拆出来单列**，因为它的验收不需要玩家实体——滚动速度改由执行得出，
在现有地图查看器里就能验完。M4a 因此瘦回出生点 / 输入 / 相机 / 碰撞。

##### 内核做完，resolver 只填最小集

| 部分 | 内容 | 伪代码 | 这一步 |
|---|---|---|---|
| **内核** | `CScript::Load` + 8 个语句的 `Execute`/`Skip` + `CScriptState` + `CScriptInterpreter` | ~2300 行（:105677–:108010） | 全做 |
| **Resolver** | 12 个类的 `FunctionResolver` / `VariableResolver` | ~2800 行，约 230 个函数 | 只做 `CLevel` 最小集 |

**Resolver 不是地基，是接口，必须跟着各子系统增量长。** `CLevel::FunctionResolver`（:117365）
有 82 个 case，绝大多数要调 `CEnemy` / `CBrother` / `CResourceLoader` / `Mission`——都是 M4、M5 的东西，
现在硬做只能写 82 个空壳。这一步只填 42 号（设置图块层滚动速度）和 8 个 `CLevel` 变量
（`CLevel::VariableResolver` :114371，很浅，全是对象里的 int16 字段），
其余调用打一行 log 记下 id 和参数就返回——log 攒出来的就是 M4 / M5 该补哪些 case。

##### 格式已解出

七张 uint8 计数前缀的表（`CScript::Load` :106317）：导出函数索引 ×2 / 资源引用 /
数据块 / 变量初值 / 状态 / 全局函数体。`CScriptCode` 是 `[uint8 字节长][uint8 语句数][语句…]`，
长度前缀让 `Skip` 成为 O(1)——条件分支的未命中臂就是这么跳过去的。

八个语句 opcode（`CScriptCode::Execute` :106801）：
`0` 函数调用、`1` 切状态、`2` 变量运算（9 种，含"右值取函数返回值"）、
`3` 事件（`Execute` 时跳过，只在 `Evaluate` 时匹配）、`4` 条件（带 else-if 链，
比较符 11 种，`6` 是无条件 else）、`5` 返回值。

操作数四种寻址（`GetData` :107478）——**这正是 M3.3 只敢认字面量的原因**：
bit15 字面量、高字节非 0 走 `ResolveVariable`、低字节 <0xFA 是脚本局部变量、
≥0xFA 是函数实参寄存器。

`CScriptState` 是**带继承的状态机**：36 字节里有帧序列、若干带 id 的导出函数、
进入块、退出块，外加一个 `parent` 字节（255 为无）。`GetSequence` / `Evaluate` / `OnExit`
查不到就沿 parent 链往上爬。

> **验收**：全部脚本资源**解析到末字节剩余 0**；干跑 6 张滚动地图的关卡脚本，
> 42 号函数拿到的数值与 M3.3 那张表**逐项相同**；`LevelScriptScan` 删除——
> 但要等这条交叉验证通过之后再删，它现在是回归基线。

> 实测：**23 个关卡模板全部解析到末字节，剩余 0 字节，0 个读越界**，其中 19 个带脚本——
> 与 M3.3 量出的密度分毫不差。`--levels` 现在解析整个模板而不是嗅探字节，这条就是
> 格式读对了的证明。
>
> 6 张滚动地图的 7 个滚动层，数值与 M3.3 那张表**逐项相同**。交叉验证跑完 22 张图
> **0 处不一致**，`LevelScriptScan` 随即删除。
>
> | 地图 | 脚本传的参数 | ×(−0.05) 之后 |
> |---|---|---|
> | pack12 map 0 | 层0 `(0, 1.5)`，层1 `(−1.199, −0.699)` | `(0, −0.075)`，`(+0.060, +0.035)` |
> | pack2 map 1 | 层0 `(0.5, 0)` | `(−0.025, 0)` |
> | pack2 map 2 | 层0 `(1.0, 0)` | `(−0.05, 0)` |
> | pack2 map 5 | 层0 `(0, 1.0)` | `(0, −0.05)` |
> | pack2 map 7 | 层0 `(1.199, 1.297)` | `(−0.060, −0.065)` |
> | pack9 map 0 | 层0 `(3.0, 3.0)` | `(−0.15, −0.15)` |
>
> **那个 255 字节上限没有成为问题**，因为编译器根本不生成长代码块：最大的脚本
> （pack12 level 0）有 **122 个状态、45 个函数**，逻辑是横着铺开的，不是纵向堆在一个块里。
> 上百状态的都是波次关，一波一个状态；其余关卡只有 1~11 个。
>
> **未实现的调用逐条记了下来，这就是 M4 / M5 的清单。** `CLevel` 的 54 号被调用 **122 次**，
> 遥遥领先，其后是 55 号（36 次）、3 号（27）、14 号（23）、6/4/2/1 号（各 19）。
> 跨类调用也有，主要落在 spawner。
>
> **文件与原始源码树一一对应**：`src/gluScript/` 的 11 个文件，按第五节的命名映射
> （小写驼峰 → `C` + 首字母大写）落成 `glu_script/` 的 11 组头 + 实现。
>
> 一处新增：`CScriptResolver.h` 里的 `IScriptObject`。原版的 host 是裸 `void *`，
> 但 `SetState`（:107323）和 `Refresh`（:107271）要调它虚表的前三项——**声明那张虚表的类
> 没能从符号表还原出来**，所以按用途起了名字。它放在 `scriptResolver` 里而不是单开一个文件，
> 因为那本来就是 gluScript 与十二个游戏类之间的那一层。
>
> `CLayerTile::SetSpeed` 收回了原版的 ×(−0.05)。M3.3 时那个因子折在调用方，理由是
> "读脚本的人已经要做一次缩放"；现在真 resolver 到位了，那个理由不成立了。

##### 顺带：地图不再画到画布边缘

`CLayerCamera`（`layerCamera.cpp`，:127663）补上了——之前只是跳过。它是一对世界像素矩形，
**地图能被看到的范围**：图块层会一直铺到画布边缘（滚动层还会环绕填充），而相机走不到那里，
原版永远不会揭穿。整图适配的查看器会，所以现在按这个矩形裁剪（`glScissor`），
适配缩放也按它算。

`CLevel` 的 **1 号函数 `setCameraLayer` 一并实现了**——参数索引的是**整个图层栈**，
不是相机层的序号（`CMap::SetCameraLayer` :92289 拿它直接索引全部图层的数组）。
22 张图里有脚本的那些全部命中，0 个落空，这本身就验证了索引语义。

> 查看器裁剪用的是**所有相机矩形的并集**，不是脚本开场选的那一个。多数地图有 2~4 个相机区域，
> 关卡推进时切换：pack2 map 0 开场那个只有 572×669，而整张图 2304×1792。
> 并集才是"游戏里能看到的一切"——22 张图**每一张的并集都小于画布**，
> 所以外沿照样砍掉，只是不会把查看器裁到关卡开场的一角。日志两个都打。

#### M3.5 — 看得到模型

Section 31 mesh 解析 + 静态模型查看器。**从 M4b 里拆出来前置**，因为它的验收不需要实体——
解析和渲染对不对，转台上一眼就看得出来，和 M3.x 那批查看器同型。

格式已解出，见 [`mesh.bt`](_Big_tool/binary%20template/big_assets/mesh.bt)（对着 `CMesh::Init` :97705 核过）：
`magic(1) + indexCount(4) + boneCount(1) + frameCount(2) + vertexCount(2)`，后接长度前缀的骨骼名、
uint16 索引缓冲、逐顶点静态 UV、逐帧数据。一帧 = `4 + 28 × boneCount + 12 × vertexCount` 字节。
寻址不用新做：`Mesh = 31` 已经在 `CGameObjectPack.h` 里，走 M3.1 那条 `ReadSectionResource`。

三件事和 2D 那条线不一样：

- **动画是逐帧整份顶点数组**，不是骨骼蒙皮。`BuildTweenFrame`（:98051）在相邻两帧间线性插值；
  骨骼的 pos + 四元数只是挂点（枪口、手），不参与变形。
- **索引缓冲是三角带**，靠重复索引造退化三角形，把多条带缝成一条。
- **贴图不在 mesh 里**。`CMesh::Init` 的第三个参数就是 `CMoveSetMesh`，贴图来自
  `CMoveSetMesh::Init`（:122930）开头那张表：uint32 packHash + uint8 数量 + 每项 2 字节。
  动作表顺手也解了（帧区间 + 速度 + 音效帧），但这一步不播，留给 M3.6。

**还有一个坑，踩到了才发现**：v 轴的原点两条线不一样。精灵 UV 是从图片**顶部**数的像素行，
和 `CTexture` 上传时 row 0 → v=0 一致；mesh UV 出自 3D 建模工具，从**底部**数。不翻转的话模型
穿着上下颠倒的图集——玩家的头去采马甲、胸口去采脸。翻转放在 `CMeshBuffer`，`CMesh` 保持纯解析。

渲染层要新加静态 VBO / IBO 和深度测试。现在这套 `CQuadBatch` 是为"每帧重填的 2D 四边形"设计的，
而 mesh 的索引缓冲和 UV 一辈子不变，两条路。

> **验收**：含 mesh 的包，其 Section 31 资源逐个解析到末字节剩 0；转台里模型带正确贴图显示，
> `--frame` 能挑任意一帧。**播放**挪到 M3.6——时间插值是独立的一块，和"解析对不对"分开验更清楚。

#### M3.6 — 动画播起来

**从 M4b 里拆出来前置**，理由和 M3.5 一样：转台上就能验，不需要实体。

一句话说清这一步和下一步的关系：**拼装依赖动画求值器，不能反过来。**
`CMeshCamera::DrawHeirarchy`（:99070）里每个部件的挂点变换来自
`CMesh::GetNodeAt(父mesh, 当前帧, 骨骼号)`，而 `GetNodeAt` 和 `GetVerticesAt`（:98486）
是同一个求值器的两个出口——都按毫秒找出夹住当前时刻的两个关键帧，再调 `BuildTweenFrame`
插值。枪要放进手里，就得先能在任意时刻求出 `hand` 骨骼的位置。

- `CMesh::GetVerticesAt` / `GetNodeAt` / `BuildTweenFrame`（:98486 / :98298 / :98051）——
  输入毫秒，输出插值顶点数组和挂点变换。骨骼走四元数插值，顶点走线性。
- `CMeshAnimationController`（:98708 ~ :98831）——持有 mesh + 帧区间 + 时间 + 循环标志，
  `Update(dt)` 推进时间，`Render` 把当前顶点推进 VBO。
- `CMoveSetMeshController::SetMove`（:134082）——把动作绑到控制器上。动作表 M3.5 已解析。

**一个坑**：`CMesh::Init` 现在走 `a3 == null` 全量路径。接上 move set 之后
`IsFrameUsedInMoves` 会跳过没人用的帧，那些帧 `timeMs` 照读、`vertices` 是空的，
`frames` 数组会出现空洞。求值器按时间查找时可能命中空帧——原版靠"只查询动作覆盖的时间段"
规避，我们得把这个不变量断言出来，否则是个偶发的空数组解引用。

> **验收**：转台里按动作播放，`M`/`N` 切动作，空格暂停、句点单步；沿用 `--advance` +
> `--screenshot`，动画不用人盯着也能验收。

#### M3.7 — 拼成一个角色

同样前置：一个完整角色在转台上就能验，不需要关卡。

- `CMatrix4d` 补 `Rotate(角度, 任意轴)` / 四元数转矩阵——原先只有单轴旋转和平移。

> **引擎的矩阵是列主序**，由 `math::operator*`（:370909）敲死：它第一个输出项是
> `L[0]*R[0] + L[4]*R[1] + L[8]*R[2] + L[12]*R[3]`，行主序在这里该读 `L[1]*R[4]`。
> 把 `DrawHeirarchy` 那 16 个内联的四元数分量按顺序抄进本项目的**行主序**布局就等于转置，
> 而旋转矩阵的转置就是逆——所以 `Matrix4dFromQuaternion` 的非对角项符号全部取反，
> 两次转置相抵。**这个错了的症状不直观**：挂件角度偏，且伸得越长越像"被整体平移了"
> 而不是"被转了"；骨骼旋转接近 0 或半圈的部件两种写法都对，因为半圈的逆就是它自己。
> 所以它一开始只在长枪和火箭炮上暴露，看着像平移 bug。客观判据：挂件应该朝体外伸，
> 78 个敌人的 38 个挂件，转置后**全部**朝外，不转置**全部**朝内。
- 部件表。**"层级"是个误称**：`DrawHeirarchy` 收的是一张扁平数组，每条 68 字节，
  内容是 `{动画控制器, 贴图, 挂点变换 pos+quat, 额外旋转 角度+轴, 闪光色}`，
  部件矩阵 = `base · T(挂点位置) · R(挂点四元数) · [R(额外)]`。
- 玩家是两个 mesh 拼的：`pack0_core` mesh 0 是上半身，mesh 1 是裤子+靴子，各带各的图集，
  枪是第三个部件。

三条挂点规则，读自 `CBrother::Draw`（:134780），一条都猜不出来：

- **挂点一律取 part 0 的 mesh 和 part 0 的时钟**。枪跟的是躯干的动画，不是自己的。
- **躯干和腿不挂任何东西**。它们的部件记录只填控制器和贴图，挂点变换留零，
  而全零四元数经那个矩阵公式恰好是单位阵。两截对得上是因为本来就画在同一空间。
- **枪的骨骼号是硬编码常量**，不是数据：单手取 4（`gun`）、左手取 2（`gunleft`），
  双持两条都用。护甲的骨骼号才是数据（存在角色对象的字节字段里），跟 armour 系统一起走。

暂不做 `DrawHeirarchy` 里那个可选的第二遍渲染（换混合模式再画一次，用部件记录尾部那个
颜色，疑似受击闪光）。

> **验收**：上半身 + 腿 + 枪拼成完整角色，跑动时枪跟着手走。

> 玩家这条线到此为止。武器有 76 套模型、护甲有 136 套，各自还要接装备系统，
> 收益都在 M5 之后——**继续挂零件不如先把敌人拼起来**，敌人是 M5 的前置。

#### M3.8 — 敌人自己拼自己

**敌人的形状不在模板里，在它自己的脚本里。** 这一条是 M3.7 之后才发现的，
也是这个里程碑存在的全部理由。

`CEnemy::Bind`（:73381）只铺 8 个空部件槽、把每个槽都指向模板里那唯一一份 move set、
把部件数设成 **1**、骨骼号全填 -1，然后就结束了。资源数据里没有任何一处说坦克有三段、
炮塔挂在哪。说这话的是脚本，在 spawn 时通过四个 native call：

| id | 作用 |
|---|---|
| `0x0A` | `SetPartCount(n)` |
| `0x0B` | `SetPart(部件, 动作)`；三参形式再加 `骨骼号`，两参形式写 -1（不挂） |
| `0x0C` | `SetPartRadius(部件, 半径)`，碰撞用，不影响绘制 |
| `0x10` | `SetPartDirection(部件, ...)`，部件的额外朝向 |

动作指向一个 mesh config，所以"选动作"就是"选这一段用哪个模型"——一份 4 config 的
move set 就这样变成机体 + 炮塔 + 两门炮。

配套发现的两件事：

- **`SpawnForUI` 就是菜单里展示敌人的整条路径**。`CMenuMeshEnemy::Bind`（:169128）只有
  `CEnemy::Bind` + `CEnemy::SpawnForUI` 两步，后者调 export 3，**不进任何状态**。
- **IDA 把 `CEnemy::GetMoveLooped`（:68817）命名错了**：它读的是动画控制器的 +32，
  也就是"刚播完"标志，不是循环标志。这个方法配上 `CEnemy::OnMoveChanged`（:68811），
  正好是 `IScriptObject` 的第二、第三个槽——对敌人来说，**状态的动画序列帧就是动作号**。

这也是 `ScriptResolver` 第二个落地的类（class 7），M3.4 只做了 CLevel。

模板里还有**两个缩放**，是同一个模型在两个地方各自的画法：

- `CEnemy::Draw`（:67499）走 **游戏内**：模型先按自身最长边归一化，再放大到模板 word 66
  个世界单位，30 度俯角。小兵 74、坦克 333、boss 500，相对大小是真的。
- `CEnemy::DrawUI`（:68451）走 **结算/菜单**：先把模型塞进给定的框，再乘模板 word 67 的
  百分比，正视角（它给 DrawHeirarchy 的 mode 传 1，拿到的是 OrientForUI）。

还有**部件自转**：部件 +136 是角速度（度/秒），不是角度。`CEnemy::Update`（:67820）每帧
做 `角度 += 速度 × 秒`，而角度没有别的写入方——所以一个部件要么永远转，要么不转，
差别只在脚本有没有调过 `0x10`。`pack1 enemy 24/26` 的刀片就是这么以 1200 度/秒 转起来的。

查看器里手动切动作需要**把 part 0 从脚本手里接管过来**（`CEnemy::SetBodyMoveLocked`，
全类唯一一处不忠于原版的地方）。原因是状态自带动画序列，`Refresh` 每次当前动作播完就
把序列的下一帧塞回来——不接管的话手选的动作转眼被顶掉，看起来就是在两个动作之间反复横跳。
接管的同时要打开循环：part 0 本来是**不循环**的，因为游戏里播完了自有脚本挑下一个。

**动画有三层，`--enemyanims` 列的是第三层**——也就是游戏真正播的那个：

| 层 | 在哪 | 是什么 |
|---|---|---|
| 1 | mesh 资源后半部分 | 逐帧的完整顶点数组 + 时间戳，姿态库 |
| 2 | 模板的 move set | **动作** = 姿态库上的一个帧区间 + 速度 + 音效帧 |
| 3 | 模板内嵌脚本的**状态** | **序列** = 一串动作号，逐个播完接下一个 |

idle / attack / 死亡 对应的是**状态**，不是动作。一个状态常常串几个动作：
`pack1 enemy 17` 的 state 4 是"动作 6（267 ms 起手）接动作 0（1067 ms 循环）"，
`enemy 24` 的 state 3 是同一个挥击动作重复四遍。

第二层的复用是实打实的：`pack0_core` mesh 0 有 403 帧，`player` 模板的 move set 用掉 149 帧，
`gun` 模板的 move set 用掉其中 43 帧——**同一份 3D 数据，两个模板，两个窗口**。
原版为此还做了优化，`CMesh::Init` 拿到 move set 时会跳过没有动作引用到的帧。

> **验收**：78 个敌人逐个跑脚本，多段的拼成整体；坦克、机甲、拿枪的兵都站得起来；
> 刀片会转；两个缩放各自生效，`G` 在两种视角之间切换；`M`/`N`（或 `--move <n>`）
> 选定的动作在 6 秒后仍然是它、且一直在循环播；`--enemyanim` 里 `M`/`N` 走状态，
> 多动作的状态会按序列自己接下去（606 个状态带动画）。

### M4 — 动得起来

**这一关拆成两半，因为角色不是精灵。** `CBrother::Template::Init`（:134571）读的是
`CMoveSetMesh`，`CBrother::Draw` 走 `CMeshCamera::DrawHeirarchy`；模板里那个
`CGameSpriteGluRef` 只在 `DrawBackground` 里用，是影子。`CGun` 和 `CEnemy` 同样是
`CMoveSetMesh`。`CMoveSet`（精灵动作集）全项目只有 `CProp` 用——也就是说
M3.2 已经把精灵动画这条线走完了，角色那条线绕不开 Section 31。

**mesh 的格式、渲染、动画、拼装已前置到 M3.5 ~ M3.7**——凡是转台上能验的都挪走了。
剩下的两半是地基和入戏：会动的角色摆在关卡里还得有相机和碰撞，而时间步、相机、碰撞
是 M5 也要踩的，所以地基仍在前面。

#### M4a — 关卡与实体骨架

`CLevel`(Section 8) + 出生点 + `input` + 相机跟随 + 碰撞层。
`CLevel::Template::Init`（:114779）很浅：地图的 `GameObjectRef` + `CScript` + 3 个 uint16，
其中 `CScript` 那一段 M3.4 已经解析并跑起来了。
出生点是对象层的 type 15（`PlacedObjectType::Player`），M3.1 已经解析到，只是当成
不可见标记跳过了。碰撞层同理——有解析器，但只跳过不保留。
`CLevel::FunctionResolver` 那 82 个 case，按关卡实际用到的补，不照 id 顺序铺。

玩家直接用 M3.5 画得出来的模型，朝向和缩放先随便摆——**占位的是姿态，不是资产**，
省掉一次性的影子精灵占位。
这一步也该把 `CCamera` / `CRenderQueue` 从 `M3Map.cpp` 里提炼出来——到这时才有
会移动的实体，队列才真的需要每帧重排。

> **验收**：一个东西在真关卡里按真碰撞跑，相机跟随。

> **已开始**。出生点其实 M3.1 就解析并保留了——`PlacedObject` 带 `objectType`
> （`Player = 15`、`Enemy = 5`）、坐标和 spawnTag，只是没人用。地图查看器加了 `K`
> 叠加层（`CMarkerBatch`，跑在已有的 constcolor 那对 shader 上，不新增第八个）
> 把它们画出来。22 张图全部有出生点数据：**每张恰好 1 个玩家出生点**
> （pack7 的 map 1/2 是 0，正是那两张图块数据全 255 的空图），
> 敌人出生点 0~28 个；为 0 的那些应该是走 `CEnemySpawner` 而不是对象层。
>
**缩放链**（`CEnemy::Draw` :67499）：
`mesh.inverseExtent × 运行时缩放 × 模板word66 × CCamera::GetScale()`，
关卡起手是 `SnapScale(camera, 0.8)`（:120747）。

> **这个乘积作用在原始顶点上，不是作用在已归一化的模型上**——`inverseExtent`
> 本身就是归一化那一步。炮塔：mesh 最长边 136.2，word66 = 150，
> `136.2 × (150/136.2) × 0.8 = 120` 世界单位；小兵：`62.5 × (74/62.5) × 0.8 = 59`。
> 按"模型已经是 1 单位"去读会得出不到一个像素的结果，差两个数量级——这就是读错时
> 画面的样子。`ComputeBounds`（:98184）确认 offset 56 是 `1/最长边`，
> `SetWidthAndHeightMappedOrthoProjection`（:378817）确认投影是 1 单位 = 1 像素。

**验证**：pack9 map 0 的两个"敌人"其实是友方炮塔，地形上画着两个圆形炮台。
把模型按对象层坐标画上去，**炮塔严丝合缝嵌进炮台**——位置和缩放同时验证通过。
这是全库唯一能验的样本：发行版全是生存模式，敌人由关卡从周围刷出，
对象层里的敌人点是没做完的战役模式遗留，别处没有参照物。

**装配走的是 export 0，不是 export 3。**
`CEnemy::SpawnForUI`（:72858）调 export 3，那是菜单的路；`CEnemy::Spawn` 的两个
重载（:73239 / :73284）都以 `CallExportFunction(interpreter, 0)` 收尾，那才是关卡
里真正生成一个敌人的路。78 个模板里有 18 个的 export 3 连一个 move 都不给 part 0——炮塔
（enemy 13/16/21/53 等）用 export 3 装出来只有一个光秃秃的底座，上半身的
`SetPartCount(2)` + `SetPart(1, move, bone)` 全在 export 0 里。

> 改用 export 0 之后：**多部件敌人从 22 个涨到 33 个，挂骨骼的部件从 38 个涨到
> 50 个，装不出任何动作的从 18 个降到 3 个**。地图走 `EnemySpawnMode::Level`；M3.8 预览器仍先跑 export 3（它模仿的
> 就是 `CMenuMeshEnemy::Bind`），只在 export 3 什么都没装出来时回落到 export 0。

**玩家站到出生点上。**`CBrother::Draw`（:134960）的缩放链和敌人同形：
`mesh.inverseExtent × this[494] × this[495] × CCamera::GetScale()`。
`CBrother::Bind`（:135608）把 this[494] 写死成 1.0，把**模板 offset 112**
写进 this[495]——那个字段原来在 `CBrother::Template` 里标着 TODO，现在叫
`m_gameScale`，值是 83。

> 和敌人唯一的差别是**支点**：`CEnemy::Draw` 给 DrawHeirarchy 传自己的包围盒中心，
> `CBrother::Draw` 传 null，让相机的默认支点顶上（这里按原点处理）。
>
> 顺手把角色装配从 M35Mesh.cpp 挪进了 `PlayerModel.{h,cpp}`——地图和 M3.7 预览器
> 现在共用同一份躯干/腿/枪骨骼逻辑。留在 M35Mesh.cpp 的是**武器目录**，那是预览器
> 的功能：游戏只会给玩家一把枪，不会给他一个列表翻。地图上玩家只挂默认模型，
> 不带武器不带装甲——挂哪把枪是 loadout 的事，loadout 还没读。

> **把 3D 模型塞进 2D 场景，踩到两个 GL 状态坑**：
>
> 1. **地图的正交投影没留深度。** `Matrix4dOrthoTopLeft` 原本 `out[10] = -1`，
>    裁剪空间 z 就等于世界 z。精灵都在 z = 0 上，感觉不到；模型有 120 世界单位厚，
>    整个被裁到只剩 |z| ≤ 1 的一薄片——pack7 map 3 的炮塔成了一块灰白方板，
>    pack9 的干脆一点不剩。加了 `depth` 参数（地图给 4096）之后就对了。
> 2. **混合开关不是我的。** `CQuadBatch::ApplyBlendMode` 只调 `glBlendFunc`，
>    `GL_BLEND` 在开场打开后整帧不动。画模型前 `glDisable(GL_BLEND)` 会双向出错：
>    后面的精灵全部丢掉 alpha 变成黑方块，模型自己的地面阴影盘也糊成一块白板。
>    正确做法是只设函数，`glBlendFunc(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA)`。
>    M3.5 / M3.8 两个模型预览器从来没开过混合，白板问题同源，一并补上。

> **踩到一个指针陷阱，值得记住**：`CEnemy::Bind` 只保存 move set 和 script 的
> **地址**。把 `EnemyTemplateData` 建成局部变量、Bind 完就出作用域，模型会指向已释放
> 的内存——表现是脚本 `Refresh()` 走进野内存后整个进程卡死，看着像死循环而不是崩溃。
> `PlacedEnemy` 因此用 `unique_ptr` 持有模板：vector 扩容时堆对象不会搬家。
>
> 顺带查清的 `CCamera` 形状（提取时照抄）：`this[1]/this[2]` 是世界坐标，
> `GetLocationX(x)` = `round(x - camera.x)`，`this[13]` 是缩放（`GetScale` 返回它），
> 构造时 `this[0] = min(屏宽/480, 屏高/320)`——**原版的参考分辨率是 480×320**。

#### M4b — 真角色

模型、动画、拼装在 M3.5 ~ M3.7 做掉了，这里剩**入戏**：`CMeshCamera::OrientForGame`
（:98959）+ 游戏相机 + 渲染队列里的 z-order + 输入驱动动作切换。
`OrientForGame` 叠了 `SetWidthAndHeightMappedOrthoProjection`，要有真实体和游戏相机
做参照才验得出对错——这是它留在这里、没跟着 M3.7 走的原因。

> **验收**：角色站在地图上，键盘/手柄能移动，播放行走动画。

#### M4.1 — 第一条可操作闭环

先把 M4a / M4b 各取一条最短路径接起来：WASD 持续输入 → 玩家位移 →
地图碰撞 → 相机跟随 → idle/run 动画。它不是完整的 `CPlayer`，但第一次让现有
资源查看器具备游戏循环，后续子系统可以围绕真实位置而不是静态出生点接入。

地图入口拆成两个明确模式。菜单 `1 Preview` 是纯预览器：玩家不响应输入，启动时像
图片编辑器一样适配整张地图，裁掉地图边界外的绘制，并保留拖动、滚轮缩放和 `Home` 复位。菜单 `2 GameView`
由游戏相机独占视角：WASD 移动并持续跟随玩家，禁止拖动、滚轮和 `Home` 改变视角。
其余菜单项统一后移一位。命令行默认 Preview，`--gameview` 显式进入 GameView。

桌面窗口不再死磕 iPad 4 的 **2048×1536**。首选 **1600×1200**，启动时读取系统可用
桌面区域；放不下就保持 4:3 等比缩小到 90% 并居中，标题栏和四条边始终可见。

碰撞层不再由 `CMap` 跳过：`CCollisionData` 保存 int32 世界坐标顶点和
`{group, first, second}` 边。关卡原生函数 2 `setCollisionLayer` 已接入，不再固定取
第一个层；`CProp::Template` 的普通/子弹碰撞也完成解析，普通碰撞按实例坐标平移后与
脚本选择的地图层合成唯一有效碰撞场景。人物碰撞半径从临时的 30 改为原版直径 22
推导出的 **11.5**。移动求解照 `CPlayer::Move` 的外层形状拆成 **5 个子步**，每步最多
**3 次**圆—线穿透修正。`C` / `--collisions` 只显示实际参与移动的有效边，不再把所有
地图层叠在一起造成“多一圈”。

PvP 掩体默认使用原版完整动画。`B` 会把当前地图上的全部掩体统一循环为完整、一级破损、
二级破损、损毁残骸、完全消失，再回到完整；损毁和消失阶段从有效碰撞场景移除，重新长出
时恢复碰撞。Preview 与 GameView 共用这套状态切换。

> 自动验证：Debug 构建 0 warning / 0 error；Preview 与 GameView 均生成 1600×1200
> 验收图。pack11、pack12、pack2、pack7、pack9 会分别选择脚本指定的碰撞层，并将
> 10～54 个 Prop 碰撞实例并入场景，均解析到末尾。WASD 的速度、朝向零点和 idle/run
> 动作槽仍需人工游玩后与原版逐项校准，所以 M4 总里程碑暂不打勾。

### M5 — 打得起来

2026-09-07：新增菜单 16 Arena，接通通用敌人的选敌、行为、攻击、受击、死亡及双方生命／伤害。
支持全目录切换、X 追加同种单位、G 无敌、红绿血条、暂停和单步，沿用玩家换枪逻辑。
78 个敌人条目与 76 个武器条目完成可重复调查；具体覆盖和缺脚本／展示资源见
[Arena 使用说明](docs/arena.md)。Boss 特殊机制、手雷眩晕、奖励和真实关卡刷怪导航仍待后续，
因此 M5 总项继续保留未完成状态。

2026-09-07：新增 `player weapon` 菜单及 GameView 装备切换、鼠标瞄准/左键开火。
修复装备脚本未执行导致的长枪固定姿态，按本体 moveset 和左右手字段绘制；
1–7 选择分类，N/M 类内切换，E 保留。资源关系、验收命令及弹体表现边界见
[玩家与武器渲染](docs/player-weapon.md)。这一步完成装备及发射预览，M5 战斗验收仍待后续。

`bullet` + `gun` + `enemy` + `enemySpawner` + `collision` + `particle`。

> **验收**：能开枪、命中、敌人死亡、有粒子特效。**到这里游戏算"能玩"。**

之后排期：音频 → 菜单（40 个文件）→ 进度存档。

---

## 五、工作方法

1. **保留原类名和文件名**。`CBrother`、`CLayerTile`、`bullet.cpp` 一个不改。符号未剥离是最大红利，名字对得上才能随时反查伪代码。命名映射：`bullet.cpp` → `CBullet`，小写驼峰 → `C` + 首字母大写。
2. **先编译通过，再重构**。允许 `*(int*)(this + 544)` 先留着，加 `// TODO: offset 544 = ?`，跑起来后再命名。
3. **二进制解析层写测试**。M1 每个结构体解析都要有断言，这是后面一切的地基。
4. **冲突时以反编译为准**。`_Big_tool` 的笔记是参考，发现差异就记回它的 md 里。

---

## 六、已知风险

| 风险 | 影响 | 应对 |
|---|---|---|
| ~~**体系外资源寻址未解**~~：已解。所谓"不在 Section 表内"的资源住在**聚合资源**里，handle bit29 标记，走 `CAggregateResource` | 已消除 | M1 实测：5659 个条目解析 5594，41 个空引用，21 个未解（仅两个 name key，疑为引擎元数据） |
| **`gluScript` 脚本系统**（11 文件 + `CScriptInterpreter`） | **绕不开，且比预想的重**；风险已从"格式未知"转成"resolver 面积大" | 密度量出来了（`--levels`）：**23 个关卡里 19 个带脚本**。13 个模板类型有 `CScript` 字段，且脚本不只做剧情——连背景滚动这种纯表现的事都走它（M3.3）。字节码格式**已全部解出**。**内核已在 M3.4 做掉**，`LevelScriptScan` 已删。剩下的面积在 resolver：12 个类约 230 个函数，只能跟着 M4 / M5 各子系统增量长，按关卡实际用到的补 |
| ~~**`spriteGlu3`** 只有 3 个文件但可能是关键~~：已解。`CGameSpriteGluRef` 走 packHash + archetype，不带资源 ID | 已消除 | M3.1 实测：22 张图 1653 个 PROP 全部画出，0 失败。特效贴图那批"体系外资源"正是走这条路 |
| **3D mesh 的渲染与姿态**（Section 31） | 挡住玩家、敌人、枪——三者都是 `CMoveSetMesh` | 风险持续收窄：格式对着 `CMesh::Init`（:97705）逐字段解出并写进 `mesh.bt`；**解析与静态渲染 M3.5 已完成**，模型带正确贴图上屏。剩下的面积拆成 M3.6（时间插值）和 M3.7（挂点拼装），两者都能在转台上验收，只有游戏相机留在 M4b |
| **顶点属性声明还是猜的** | 只影响忠实度，暂不影响画面 | 带 `TexCoord` 的 6 个 shader 全都乘 `1/4096`（精灵 UV 是整数），但 mesh UV 在文件里是 0~1 的浮点数——按理会被缩到 1/4096，整个模型只采到左上角一个像素。现在靠 `CMeshBuffer` 上传前 ×4096 抵消掉，画面正确，但说明 `CVertexBuffer` 那个格式串（`"\b"`）我们还没解开。M3.7 接矩阵栈时会再撞上，那时一并查 |
| ~~**寻路图层格式未解**（`CLayerPathLink` type 5 / `CLayerPathMesh` type 6）~~：已解，见 `_Big_tool` 的 `map.bt` | 已消除 | M3.1 补齐了两者的解析（只跳过不保留）。之前遇到它们就停，藏在后面的对象层读不到 |

---

## 七、进度

- [x] M0 骨架 —— 2026-09-05 完成
- [x] M1 读得到资源 —— 2026-09-05 完成
- [x] M2 看得到贴图 —— 2026-09-05 完成
- [x] M3 画得出地图 —— 2026-09-05 完成
- [x] M3.1 地图上的装饰物 —— 2026-09-05 完成
- [x] M3.2 地图活起来 —— 2026-09-05 完成
- [x] M3.3 背景流动起来 —— 2026-09-06 完成
- [x] M3.4 脚本跑起来 —— 2026-09-06 完成
- [x] M3.5 看得到模型 —— 2026-09-06 完成
- [x] M3.6 动画播起来 —— 2026-09-06 完成
- [x] M3.7 拼成一个角色 —— 2026-09-06 完成
- [x] M3.8 敌人自己拼自己 —— 2026-09-06 完成
- [ ] M4 动得起来
- [x] M4.1 第一条可操作闭环 —— 2026-09-07 完成（Preview/GameView 已拆分，动作手感待校准）
- [ ] M5 打得起来
- [x] M5.1 Arena 通用战斗测试 —— 2026-09-07 完成（双方伤害、玩家生命、阵营过滤和全目录回归；边界见 `docs/arena.md`）

### 武器预览反馈修正（2026-09-07）

共享玩家装配与目录适配移至 `runtime`，武器弹体/粒子整合归 `gun_bros/WeaponEffects`；查看器和回归入口保留在 `milestones`。
修复粒子动画掩码、零间隔过量发射、弹图旋转、转台投影、光束拼接和独立子弹碰撞层。
操作、音效核对与未实现边界见 `docs/player-weapon.md`；M5 战斗仍未完成。

后续反馈：接通玩家动作帧 WAV，修复喷火持续发射、弹体首帧与遮挡顺序、导弹 mesh 缩放，并统一各图 GameView 视野。


### 2026-09-09 已验证细分项（不替代上文整体验收）

- [x] G2.1 正式四生存星球按Planet→Mission→LEVEL→Map引用进入；原装备/枪槽四星各2波、原格式保存重载通过。教程/BOKOR/星图旧关联仍待核对。
- [x] G3.1 移除脚本状态数量启发式，Mission.type1和明确LEVEL引用选择；历史archive由独立参数区分。四星回归退出0，完整旧战役入口仍需专项复核。
- [x] G9.1 18类原格式存档读写、原构造默认值/BIG初始装备、源不存在时创建；未知字段保留、CRC拒绝和语义重载验证通过。源21文件哈希无变化。
- [x] G9.2 原秒数签到累计、重复领取限制、初始content seen位图与原赠送物品分支；边界和重载通过。其余统计/设置/活动仍未完成。
- [x] GUI主程序与独立Console研究EXE分离，里程碑永久保留，正式runtime不包含milestones；无工具EXE也能启动商店，GUI研究转发与完整菜单实战回归退出0。

详细命令和未验证边界见[本轮记录](docs/ui-original-overnight-2026-09-09.md)。

### 2026-09-09 后续已验证细分项

- [x] G2.2 原星图、Planet.mapSlot、Mission脚本选择与十轮五十波页面；BOKOR独立LEVEL、慢动作释放和高分重载。
- [x] G7.1 STORE商品卡／熟练度、原六态升级弹窗、Bank原目录与离线交易提示；人物整页投影、原UI export9和双枪脚本时序。大枪特定姿态、全页惯性和所有退场仍待核对。
- [x] G7.2 原Header七导航、星图／任务、签到／选兄弟、精炼、设置／帮助、离线社交、结算／图库、暂停、HUD通知及战斗道具选择器均新增原资源专项。永久研究入口现为1–68。
- [x] G9.3 原p音效／音乐／停靠设置、1008精炼、1009签到、BOKOR独立进度／高分、缺源新账户实际教程与所得装备重载。
- [ ] G7整体验收：仍需对原版逐帧核对惯性、回弹、全部退场、部分粒子、模型姿态和战斗内Bank返回链；不以专项通过替代完整1:1。
- [ ] 原联网、好友、多人同步与所有存档客户端业务仍未完成；保留未知字段和明确不可用状态。

当前启动及人工验收步骤已更新为[2026-09-09验收](docs/acceptance.md)，不再使用旧文本账户或GAMES→BOKOR描述。

### 2026-09-09 用户商店／Splash反馈

- [x] 第一排数量圆标被切：STORE_SCROLL输入区域误作纵向裁剪，实测切掉6个逻辑像素；恢复原回调允许角标外伸的绘制方式，仅保留横向裁剪，不改卡片坐标或Sprite尺寸。
- [x] Starter Pack仅GUNS显示；按原STORE引用记入1018，拒绝重复扣款。2026-09-09 按用户澄清纠正为本次保留OWNED、重启隐藏，并补齐礼包自动装备及Hunter盔甲已购可见规则，见[购买流程纠正](docs/starter-pack-purchase-2026-09-09.md)。原value=0代表新记录的值，键存在才是已购依据。
- [x] 视频完成后进入Splash等待，新增用户要求的font11闪烁提示；点击后继续原首次选人／老账户签到。专项和正常GUI实际点击通过。

本轮边界、原函数依据、截图和测试见[反馈修复记录](docs/ui-feedback-2026-09-09.md)。旧版未写购买记录的历史礼包不根据装备反推，不改源存档。

### 2026-09-09 PLAY交互反馈

- [x] 默认选择首个星球；补齐模式选择原粒子效果，引用来自原程序静态表、参数从BIG读取。
- [x] 左侧返回按钮读取原隐藏触摸区域；展开详情可点击收起，再点击回到星图。
- [x] REV／波次／商店支持连续拖动和松手惯性，按原Movie章节映射多页位置；移除单手势仅翻一页的适配。
- [x] Release／Debug、专项输入回归、2000波次与10Horde按钮回归、礼包回归以及正常窗口实际鼠标验证。平台减速适配和验证边界见[PLAY交互记录](docs/play-interaction-2026-09-09.md)。

### 2026-09-09 战斗 HUD 按钮反馈

- [x] 红色感叹号接原动画 27/28，选择器打开期间保持按压，关闭恢复；蓝色切枪接原动画 35/36，按住／松开切换并维持单次切枪。BIG 帧缓冲差异专项先失败后通过，Release 构建、道具选择器与暂停回归通过，见[验证记录](docs/hud-buttons-2026-09-09.md)。

收尾验证：OriginalUI18/18、Core20/20按预期；原PROP43异常仍明确保留。商店原按钮→PLAYER Flow→原1001保存重载、76款BIG武器原UI实拍通过，已从全目录定位IMG_0800为Ion Blaze（pack5:31，特殊类）。后续模型比对应使用同枪同盔甲及相同动作帧，不再从不同装备截图猜比例。图像验收索引及原件SHA结果见[本轮交接](docs/ui-original-overnight-2026-09-09.md)。
