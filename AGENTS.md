# Gun Bros 逆向重建约定

## 工作目标与授权

- 最终交付可玩的 Windows 游戏，行为和数据尽量对应 iOS 原版。
- 正式版主线：每个星球仅一张生存地图、四面刷怪，共 50 波 × 10 轮（用户补充）。优先完成这个闭环。战役模式在 1.0.0 前已废弃；可恢复为独立研究入口，不能当作正式版必经流程。
- 2026-09-08 用户授权睡眠期间约八小时自主推进；产品、技术细节与下一阶段由代理决定，早晨验收。无需就已授权的阶段重复等待批准。
- 执行顺序：调研原版依据 → 记录阶段方案与验收标准 → 分解任务 → 实现 → 测试 → 更新结果，再进入下一阶段。
- 每阶段都验证，失败先定位修复；不得将全部改动积累到最后一次测试。
- 必要的独立研究能力做成 milestone 并加入 EXE 菜单；游戏完成后仍永久保留已有里程碑及单项研究入口。
- 常规运行、截图及自动测试统一传入 `--mute`；静音不得关闭游戏逻辑或跳过资源验证。

## 事实来源

- 当前实际资源根目录为 `E:\coding_projects\c_projects\gun_bro_re`。用户消息中的 `E:\coding\_projects\c\_projects\gun\_bro\_re` 是未找到的路径写法；已在当前根目录找到对应资料。
- `gunbros`：带符号的 iOS 主程序；`_IDA_OUT/gunbros_3.6.0_IOS.c` 与 `_IDA_OUT/source_tree.md` 用于核对逻辑和原始命名。
- `10-17-2011/`：官方旧 PC 版，用户估计约 2.4.0，内容不完整且有 bug；版本号未经证实。仅作补充参照。
- `saves/`：原版存档，包含用户制作的完美存档。原件只读，实验使用副本。
- `flow scripts/`：版本未知的脚本文本，用户明确仅供参考；不能优先于当前 iOS 二进制和 BIG 中实际编译脚本。
- 类名、文件名尽量对应原版符号；有证据时纠正偏离，同时保留注释、更新引用及工程文件。新增宿主适配层明确职责，不冒充原版类。
- 无法解释的内容记录证据、尝试、影响与待用户核对项；可暂按疑似废案隔离，但不能将缺实现直接断言为原版废案。

## 编码与文档

- 回复、方案、任务清单和研究文档使用中文。Implementation Plan, Task List and Thought in Chinese。
- KISS，可读性优先；使用含义明确的命名和基础控制流，精准处理异常，保留用户全部注释。
- 必要的注释和 docstring 说明原版依据、单位与原因；必要日志用简明英文。
- Python 使用项目 `.venv`（存在时先激活），路径优先 `pathlib`；避免推导式、三元运算与 lambda。大量常量集中在文件开头。
- 项目技能放 `.agents/skills/<skill_name>/`。
- 修改前读相关文档：总体状态见 `PLAN.md`；武器见 `docs/player-weapon.md`；战斗见 `docs/arena.md`；本次阶段计划、验证和交接见 `docs/overnight-progress.md`。
- 盔甲相关工作读 `docs/armor.md`，特别注意双模型并非总是两位兄弟变体。

## 验收与交接

- 测试保留命令、退出码和必要截图；数据缺失、未验证行为和版本差异明确列出。
- 不把成功构建、模型查看器或空场地战斗等同于完成游戏；可玩验收需覆盖进入关卡、移动射击、敌人调度、受伤死亡、重开和进度反馈。
- 优先完成闭环，再补细节；保留每阶段已有验证能力。原版资源及存档不提交版本库。
